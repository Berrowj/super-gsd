---
phase: 6
plan: 1
type: auto
autonomous: true
wave: 1
depends_on: []
requirements: [VIS-01, VIS-02, VIS-03]
files_modified:
  - .planning/phases/06-overwatcher-monitoring/06-VERIFICATION.md
---

# Plan 06-01: Validate Overwatcher and Mission Control

## Objective

Confirm VIS-01, VIS-02, and VIS-03 pass by inspecting existing artifacts. Update planning-reader.js if data is missing from the signal map. Create VERIFICATION.md.

## Tasks

### Task 1: Validate VIS-01 + VIS-02

Inspect `.planning/overwatcher/signal-map.html` and `super-gsd/overwatcher/overwatcher-launcher.js`:
- Phase grid present (`.phase-grid` div with phase cards)
- Collision section rendered (collision detection logic + HTML block)
- Dependency table present (`<h2>Dependency Graph</h2>` with table)
- Decision log present (decisions table in renderer)

Check planning-reader.js for completeness:
- Reads phases from ROADMAP.md checkboxes
- Reads plans from phase directories
- Detects file collisions via `writes_to` edges
- Reads decisions from `.planning/decisions/`

Verdict: PASS or list missing items and fix them.

### Task 2: Validate VIS-03

Inspect `super-gsd/workflows/mission-control.md`:
- Dashboard bash script present with `while true; do ... sleep 10 ... done` loop
- ATC log script present
- tmux launch command present
- Reads `.planning/STATE.md`, ROADMAP.md, token-log.jsonl, git log
- `Refresh: 10s` shown in output

Verdict: PASS or list missing items and add them.

### Task 3: Create VERIFICATION.md

Write `.planning/phases/06-overwatcher-monitoring/06-VERIFICATION.md` with checklist for all 3 requirements.

## Success Criteria

- VIS-01: signal-map.html contains phase-grid, collision section, dependency table
- VIS-02: signal-map.html shows phase progress counts, plan status, collision count, decisions count
- VIS-03: mission-control.md contains dashboard script with 10s refresh, ATC log script, tmux launch command
- VERIFICATION.md written and all items checked
