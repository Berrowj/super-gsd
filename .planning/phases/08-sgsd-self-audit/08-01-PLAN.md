---
phase: 8
plan: 1
name: SGSD Self-Audit — Gap Report
type: auto
autonomous: true
requirements: [AUDIT-01, AUDIT-02, AUDIT-03]
---

# Plan 08-01: SGSD Self-Audit — Gap Report

## Objective

Read every shipped component of the Super GSD framework (skills, agents, scripts, tools,
hooks, config, docs), cross-reference each against its callers and install pathways, and
compile all findings into a structured audit report at `docs/audits/2026-04-12-sgsd-gap-audit.md`.

**DOCS ONLY — no framework code changes permitted. Any code edit is a DEVIATION.**

## Context

The researcher surveyed 38 files across 7 scope areas and pre-identified 12 gaps (G1-G12),
documented in `08-RESEARCH.md`. The research provides a per-finding output format, grep
patterns for each gap class, and specific line-number references that the executor must
confirm. The wave breakdown below follows the researcher's proposal.

Key pre-confirmed gaps to verify with exact line numbers:
- `config.browser_verify.*` read by sgsd-orchestrate and phase-verifier but absent from
  planning-config-overlay.json (G1 — critical)
- `brv-query` / `brv-curate` used as bare commands across skills and CLAUDE-OVERLAY.md but
  not on PATH (G3 — critical)
- sgsd-activity-logger.js and sgsd-statusline.js not registered in settings-overlay.json (G4/G5)
- Board agents (sgsd-board-*.md) installed but dispatched inline without subagent_type binding (G7)

## Must-Haves

At the end of this plan the following must be TRUE:

1. `docs/audits/2026-04-12-sgsd-gap-audit.md` exists with all 9 required sections
2. Report contains ≥ 10 findings, each with file path + line number + severity
3. Every "referenced but not implemented" finding cites the exact file:line of the reference
   AND demonstrates the implementation is absent
4. Every "duplicate/conflict" finding shows both instances side-by-side
5. No files outside `docs/audits/` and `.planning/phases/08-*/` are modified
6. ATC review finds zero critical rule violations in the report

## Tasks

### Task 1 (Wave 1): Skills + Agents Inventory

Read all 9 `super-gsd/skills/*/SKILL.md` files. Read all 7 `super-gsd/agents/*.md` files.
Cross-reference every agent dispatch against what is installed in `.claude/agents/` and
`.claude/commands/`. Check whether board agents use `subagent_type` frontmatter or are
dispatched purely by inline description (resolves research assumption A1). Flag sgsd-ceo
dispatch path discrepancy (dispatch-table.md vs sgsd-deliberate SKILL.md). Flag VTPidea.md
orphan in `.claude/commands/`. Flag gsd-list-phase-assumptions reference in
sgsd-orchestrate SKILL.md line 562 against installed commands list.

Record findings using the per-finding format from research section 3:
```
FINDING-{N}
Scope: Skills|Agents
Severity: critical|high|medium|low
File: path/to/file:NN
Evidence: "[quoted text]"
What's Missing: one sentence
Suggested Fix: one sentence
```

Append Wave 1 findings to `.planning/phases/08-sgsd-self-audit/scratch-findings.md`
(create if absent). Do NOT write to any other file.

Files to read:
`super-gsd/skills/sgsd-orchestrate/SKILL.md`,
`super-gsd/skills/sgsd-deliberate/SKILL.md`,
`super-gsd/skills/sgsd-browser/SKILL.md`,
`super-gsd/skills/sgsd-brv-setup/SKILL.md`,
`super-gsd/skills/sgsd-pause/SKILL.md`,
`super-gsd/skills/sgsd-resume/SKILL.md`,
`super-gsd/skills/sgsd-token-audit/SKILL.md`,
`super-gsd/skills/sgsd-transition/SKILL.md`,
`super-gsd/skills/sgsd-overwatcher/SKILL.md`,
`super-gsd/agents/sgsd-board-architect.md`,
`super-gsd/agents/sgsd-board-contrarian.md`,
`super-gsd/agents/sgsd-board-moonshot.md`,
`super-gsd/agents/sgsd-board-pragmatist.md`,
`super-gsd/agents/sgsd-ceo.md`,
`super-gsd/agents/sgsd-classifier.md`,
`super-gsd/agents/sgsd-context-selector.md`,
`super-gsd/workflows/dispatch-table.md`

Done when: scratch-findings.md contains 5-8 Wave 1 findings with file:line citations.

### Task 2 (Wave 2): Scripts + Tools Audit

Read the header and parameter blocks (first 40 lines) of all 10 scripts in
`super-gsd/scripts/`. Read key sections of `super-gsd/tools/phase-verifier/phase-verifier.mjs`
(input expectations, output format) and its `README.md`. Cross-reference each script against
coverage in `super-gsd/docs/MONITORING-SETUP.md` and `super-gsd/docs/SGSD-WORKSPACE-GUIDE.md`.
Identify sgsd-dashboard.ps1, sgsd-live-feed.ps1, and sgsd-thinking.ps1 as undocumented and
check whether sgsd-dashboard.ps1 is a predecessor to sgsd-mission-control.ps1 (compare first
30 lines of each to determine: predecessor to delete or parallel variant to document).
Note phase-verifier Linux/macOS untested status.

Append Wave 2 findings to `.planning/phases/08-sgsd-self-audit/scratch-findings.md`.

Files to read:
`super-gsd/scripts/sgsd-mission-control.ps1` (first 40 lines),
`super-gsd/scripts/sgsd-narrative.ps1` (first 40 lines),
`super-gsd/scripts/sgsd-gate-verdict.ps1` (first 40 lines),
`super-gsd/scripts/sgsd-agent-dashboard.ps1` (first 40 lines),
`super-gsd/scripts/sgsd-agent-dashboard.sh` (first 40 lines),
`super-gsd/scripts/sgsd-headless.ps1` (first 40 lines),
`super-gsd/scripts/sgsd-headless.sh` (first 40 lines),
`super-gsd/scripts/sgsd-dashboard.ps1` (first 40 lines),
`super-gsd/scripts/sgsd-live-feed.ps1` (first 40 lines),
`super-gsd/scripts/sgsd-thinking.ps1` (first 40 lines),
`super-gsd/tools/phase-verifier/phase-verifier.mjs`,
`super-gsd/tools/phase-verifier/README.md`,
`super-gsd/docs/MONITORING-SETUP.md`,
`super-gsd/docs/SGSD-WORKSPACE-GUIDE.md`

Done when: scratch-findings.md has additional 4-6 Wave 2 findings with file:line citations.

### Task 3 (Wave 3): Hooks + Config Audit

Read all 7 hook files in `super-gsd/hooks/` (header comments only — first 20 lines each).
Read `super-gsd/config/settings-overlay.json` in full. Read
`super-gsd/config/planning-config-overlay.json` in full. Read
`super-gsd/config/model-routing.json` in full.

Cross-reference: which hooks are registered in settings-overlay.json vs. not (confirm G4/G5
with exact line numbers). For every config key read by any skill or script (from Wave 1 grep
results), verify it exists in planning-config-overlay.json. Specifically confirm G1
(`config.browser_verify.*`) and G2 (`config.workflow.nyquist_validation`) are absent.
Confirm G3 (`brv-query` / `brv-curate` not on PATH) by checking install.sh alias creation.
Verify whether brv-curate-local.js is installed to `~/.claude/hooks/` or only to
`~/.claude/templates/overwatcher/` (resolves research open question 2).

Append Wave 3 findings to `.planning/phases/08-sgsd-self-audit/scratch-findings.md`.

Files to read:
`super-gsd/hooks/gsd-checkpoint-writer.js` (first 20 lines),
`super-gsd/hooks/gsd-context-monitor.js` (first 20 lines),
`super-gsd/hooks/gsd-session-start.js` (first 20 lines),
`super-gsd/hooks/gsd-stuck-detector.js` (first 20 lines),
`super-gsd/hooks/gsd-token-logger.js` (first 20 lines),
`super-gsd/hooks/sgsd-activity-logger.js` (first 20 lines),
`super-gsd/hooks/sgsd-statusline.js` (first 20 lines),
`super-gsd/config/settings-overlay.json`,
`super-gsd/config/planning-config-overlay.json`,
`super-gsd/config/model-routing.json`,
`super-gsd/install.sh` (lines covering alias creation and brv-curate-local.js install)

Done when: scratch-findings.md has additional 5-7 Wave 3 findings covering hooks and config keys.

### Task 4 (Wave 4): Docs Audit

Read `super-gsd/README.md`, `super-gsd/USER-GUIDE.md`, `super-gsd/CLAUDE-OVERLAY.md` (full),
and `super-gsd/docs/SESSION-DEBRIEF.md`. Grep for: every `brv-query` and `brv-curate`
occurrence (confirms G3 scope in docs), every reference to a command that is not installed
(e.g., gsd-discuss-phase, gsd-plan-phase, gsd-execute-phase in USER-GUIDE.md line 363-365),
and any path references to `.gsd/` that should have been updated to `.planning/`. Check
SESSION-DEBRIEF.md for project-specific content (VTP references — G12). Flag stale references
and dead links with exact line numbers.

Append Wave 4 findings to `.planning/phases/08-sgsd-self-audit/scratch-findings.md`.

Files to read:
`super-gsd/README.md`,
`super-gsd/USER-GUIDE.md`,
`super-gsd/CLAUDE-OVERLAY.md`,
`super-gsd/docs/SESSION-DEBRIEF.md`

Done when: scratch-findings.md has additional 4-5 Wave 4 findings with file:line citations.
Total across all waves: ≥ 18 findings (minimum required for the report to meet the ≥ 10 bar
after deduplication).

### Task 5 (Wave 5): Report Assembly

Read `scratch-findings.md` in full. Deduplicate overlapping findings (e.g., brv-query
appears in both Config and Docs — merge into one finding with multiple evidence lines).
Assign final severity using the CONTEXT.md severity bar: CRITICAL = user-facing behaviour
broken; HIGH = internal coherence broken; MEDIUM = stale docs; LOW = cosmetic.

Create `docs/audits/` directory if absent. Write
`docs/audits/2026-04-12-sgsd-gap-audit.md` with exactly these 9 sections in order:

1. **Summary** — overall health assessment, top 5 findings by severity, critical count, total findings count
2. **Skills Audit** — table: SKILL.md name · wired (Y/N) · invoked by · notes
3. **Agents Audit** — table: agent name · called by · orphan (Y/N) · notes
4. **Scripts Audit** — table: script · input sources · output format · caller · documented?
5. **Tools Audit** — table: tool · invocation path · known callers · untested platforms
6. **Hooks Audit** — table: hook file · in settings-overlay (Y/N) · event type · status
7. **Config Audit** — table: key · default · read by · written by · present in overlay (Y/N)
8. **Docs Audit** — list of dead links, stale refs, and missing sections with file:line citations
9. **Recommendations** — ranked list of all findings by severity then effort (critical first),
   each with: Finding ID · Scope · Severity · Effort (low/med/high) · One-line fix

Every finding in the report must use the exact format from research section 3. Minimum 10
findings after deduplication (confirmed: pre-identified gaps G1-G12 alone satisfy this).

Do NOT modify scratch-findings.md after the report is written — it is the raw working file
and must be preserved for traceability.

Done when: `docs/audits/2026-04-12-sgsd-gap-audit.md` exists with all 9 sections and
≥ 10 deduplicated findings.

## Verification

```bash
# Report file exists
ls docs/audits/2026-04-12-sgsd-gap-audit.md

# All 9 section headers present
grep -c "^## " docs/audits/2026-04-12-sgsd-gap-audit.md
```

Expected for second command: 9

```bash
# Minimum finding count (FINDING-N entries)
grep -c "^FINDING-" docs/audits/2026-04-12-sgsd-gap-audit.md
```

Expected: >= 10

```bash
# No files outside permitted paths were modified
git diff --name-only HEAD | grep -v "^docs/audits/\|^\.planning/phases/08-"
```

Expected: empty output (zero matches)

## Success Criteria

- `docs/audits/2026-04-12-sgsd-gap-audit.md` exists with 9 section headers
- Report contains ≥ 10 findings, each with file:line citation and severity label
- Every critical and high finding includes quoted evidence from the source file
- No framework files outside `docs/audits/` and `.planning/phases/08-*/` modified
- scratch-findings.md preserved as raw working file
