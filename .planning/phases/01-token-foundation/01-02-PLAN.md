---
phase: 01-token-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - ~/.claude/get-shit-done/bin/lib/model-profiles.cjs
  - ~/.claude/get-shit-done/bin/lib/config.cjs
  - ~/.claude/get-shit-done/references/agent-contracts.md
  - super-gsd/skills/gsd-orchestrate/SKILL.md
autonomous: true
requirements:
  - SAFE-03
  - ORCH-10
  - ORCH-08

must_haves:
  truths:
    - "model-profiles.cjs contains Super GSD agent roles wrapped in SUPER-GSD-START/END markers"
    - "config.cjs routes to Super GSD model routing table when .planning/config.json is present"
    - "agent-contracts.md has entries for gsd-orchestrate, gsd-classifier, gsd-context-selector"
    - "Orchestrate skill handles @file: prefixed Bash output without silent JSON.parse failure"
    - "Running /gsd-token-audit against a populated token-log.jsonl returns a formatted usage breakdown"
  artifacts:
    - path: "~/.claude/get-shit-done/bin/lib/model-profiles.cjs"
      provides: "GSD 1.0 agent profiles + Super GSD roles"
      contains: "SUPER-GSD-START"
    - path: "~/.claude/get-shit-done/bin/lib/config.cjs"
      provides: "Config resolution with Super GSD routing table awareness"
      contains: "SUPER-GSD-START"
    - path: "~/.claude/get-shit-done/references/agent-contracts.md"
      provides: "Completion markers for new Super GSD agents"
      contains: "gsd-orchestrate"
    - path: "super-gsd/skills/gsd-orchestrate/SKILL.md"
      provides: "Orchestrate skill with @file: IPC guard at every Bash result capture"
      contains: "@file:"
  key_links:
    - from: "config.cjs getModelForAgent()"
      to: ".planning/config.json model_routing"
      via: "SUPER-GSD patch: check for super-gsd routing table before GSD 1.0 lookup"
    - from: "gsd-orchestrate SKILL.md Bash capture"
      to: "gsd-tools.cjs @file: temp file"
      via: "if [[ $VAR == @file:* ]]; then VAR=$(cat ${VAR#@file:}); fi guard"
---

<objective>
Apply idempotent SUPER-GSD-START/END marker patches to 3 GSD 1.0 core files, add @file: IPC guard to the orchestrate skill, and validate the token audit skill against a real log entry.

Purpose: The SUPER-GSD patches let install.sh detect and skip already-applied patches (idempotent). The @file: guard prevents silent dispatch failures when gsd-tools output exceeds 50KB. Token audit validation proves ORCH-08 works end-to-end after plan 01-01 wired the logger.
Output: 3 patched GSD 1.0 files, hardened orchestrate skill, token audit smoke test confirmed.
</objective>

<execution_context>
@C:/Users/jack.berrow/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@C:/Users/jack.berrow/GSDedits/.planning/ROADMAP.md
@C:/Users/jack.berrow/GSDedits/.planning/phases/01-token-foundation/01-CONTEXT.md
@C:/Users/jack.berrow/GSDedits/.planning/phases/01-token-foundation/01-RESEARCH.md
@C:/Users/jack.berrow/GSDedits/.planning/phases/01-token-foundation/01-01-SUMMARY.md

<interfaces>
<!-- SUPER-GSD marker convention -->
Code files (JS/CJS): // SUPER-GSD-START ... // SUPER-GSD-END
Markdown files: <!-- SUPER-GSD-START --> ... <!-- SUPER-GSD-END -->

<!-- Idempotency check pattern (used by install.sh — replicate in patches) -->
grep -q "SUPER-GSD-START" "$FILE" || patch_file "$FILE"

<!-- @file: IPC guard — insert after every gsd-tools Bash capture in orchestrate skill -->
if [[ "$VAR" == @file:* ]]; then VAR=$(cat "${VAR#@file:}"); fi

<!-- model-profiles.cjs patch block: add after existing MODEL_PROFILES object, before module.exports -->
// SUPER-GSD-START: Super GSD agent roles
// These extend GSD 1.0 profiles. Do not edit manually — managed by install.sh.
const SUPER_GSD_PROFILES = {
  'gsd-orchestrate':         { quality: 'opus',   balanced: 'opus',   budget: 'sonnet', adaptive: 'opus'   },
  'gsd-classifier':          { quality: 'haiku',  balanced: 'haiku',  budget: 'haiku',  adaptive: 'haiku'  },
  'gsd-context-selector':    { quality: 'haiku',  balanced: 'haiku',  budget: 'haiku',  adaptive: 'haiku'  },
};
Object.assign(MODEL_PROFILES, SUPER_GSD_PROFILES);
// SUPER-GSD-END

<!-- config.cjs patch block: add routing table override inside getModelForAgent (or equivalent function) -->
// SUPER-GSD-START: Super GSD model routing table override
// When .planning/config.json exists with model_routing, use it for Super GSD agents.
const _sgsdConfigPath = path.join(planningRoot(), 'config.json');
if (fs.existsSync(_sgsdConfigPath)) {
  try {
    const _sgsdConfig = JSON.parse(fs.readFileSync(_sgsdConfigPath, 'utf8'));
    if (_sgsdConfig.model_routing && _sgsdConfig.model_routing[agent]) {
      return _sgsdConfig.model_routing[agent];
    }
  } catch {}
}
// SUPER-GSD-END

<!-- agent-contracts.md patch block -->
<!-- SUPER-GSD-START -->
## Super GSD Completion Markers

| Skill | Completion Signal |
|-------|------------------|
| gsd-orchestrate | `ORCHESTRATOR-CHECKPOINT.md` updated OR text "Dispatch complete" |
| gsd-classifier | Structured JSON output: `{ "tier": "skip|lite|full|gate" }` |
| gsd-context-selector | Structured output: `INJECTING: [list of context snippets]` |
<!-- SUPER-GSD-END -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply SUPER-GSD marker patches to 3 GSD 1.0 files</name>
  <files>
    ~/.claude/get-shit-done/bin/lib/model-profiles.cjs,
    ~/.claude/get-shit-done/bin/lib/config.cjs,
    ~/.claude/get-shit-done/references/agent-contracts.md
  </files>
  <action>
    Before modifying each file, check if "SUPER-GSD-START" already exists in it. If it does, skip that file — the patch is already applied (per SAFE-03 idempotency rule).

    model-profiles.cjs (per SAFE-03 + D-001):
    - Locate the closing `};` of the MODEL_PROFILES object and the `const VALID_PROFILES` line.
    - Insert the SUPER_GSD_PROFILES block (from interfaces above) between them — after the `};` closing the MODEL_PROFILES object, before `const VALID_PROFILES`.
    - The block adds gsd-orchestrate (opus), gsd-classifier (haiku), gsd-context-selector (haiku) roles using Object.assign so VALID_PROFILES picks them up automatically.

    config.cjs (per SAFE-03 + D-001):
    - Find the function that resolves which model to use for an agent (look for references to MODEL_PROFILES or getAgentToModelMapForProfile). Read the relevant function body first.
    - Insert the routing table override block at the TOP of that function body, before the existing logic, so Super GSD agents resolve from .planning/config.json first and fall through to GSD 1.0 logic if not found.
    - Use the `path` and `fs` modules (already required at top of file) — do NOT add new requires.

    agent-contracts.md (per SAFE-03):
    - Append the Super GSD completion markers block to the END of the file.
    - Use HTML comment markers: `<!-- SUPER-GSD-START -->` ... `<!-- SUPER-GSD-END -->`.

    Critical: Do NOT change any existing logic, exports, or GSD 1.0 agent entries. Only add — never modify or remove existing content.
  </action>
  <verify>
    <automated>grep -q "SUPER-GSD-START" ~/.claude/get-shit-done/bin/lib/model-profiles.cjs &amp;&amp; echo "model-profiles: OK" || echo "model-profiles: MISSING"</automated>
    <automated>grep -q "SUPER-GSD-START" ~/.claude/get-shit-done/bin/lib/config.cjs &amp;&amp; echo "config: OK" || echo "config: MISSING"</automated>
    <automated>grep -q "SUPER-GSD-START" ~/.claude/get-shit-done/references/agent-contracts.md &amp;&amp; echo "agent-contracts: OK" || echo "agent-contracts: MISSING"</automated>
    <automated>grep -q "gsd-orchestrate" ~/.claude/get-shit-done/bin/lib/model-profiles.cjs &amp;&amp; echo "orchestrate-role: OK" || echo "orchestrate-role: MISSING"</automated>
    <automated>node -e "require('C:/Users/jack.berrow/.claude/get-shit-done/bin/lib/model-profiles.cjs'); console.log('syntax: ok')"</automated>
  </verify>
  <done>All 3 GSD 1.0 files contain SUPER-GSD-START markers. model-profiles.cjs loads without syntax error. Existing GSD 1.0 entries unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Add @file: IPC guard to orchestrate skill + validate token audit</name>
  <files>super-gsd/skills/gsd-orchestrate/SKILL.md</files>
  <action>
    Part A — @file: IPC guard (per ORCH-10):

    Read super-gsd/skills/gsd-orchestrate/SKILL.md. Locate every code block that captures output from a `node "$GSD_TOOLS"` or `node gsd-tools.cjs` Bash call into a variable (e.g. `INIT=$(node ...)`, `RESULT=$(node ...)`, `DIGEST=$(node ...)`).

    Immediately after EACH such capture line, insert:
    ```bash
    if [[ "$VARNAME" == @file:* ]]; then VARNAME=$(cat "${VARNAME#@file:}"); fi
    ```
    replacing VARNAME with the actual variable name used on that line.

    Also add a comment above the first occurrence:
    ```bash
    # @file: IPC guard: gsd-tools outputs @file:/path when JSON >50KB — read the file
    ```

    If gsd-orchestrate/SKILL.md does not yet exist (new skill file), create it with the correct structure per the compressed-plan.xml template, including the @file: guard in its Bash capture section. Base the skill objective on: "Autonomous dispatch loop — reads STATE.md, selects model from config, dispatches sub-agent, processes structured report, commits atomically."

    Part B — Token audit smoke test (per ORCH-08):

    After confirming plan 01-01 wired the token logger, verify the audit skill works:
    1. Check `.planning/metrics/token-log.jsonl` exists and has at least 1 entry (from plan 01-01 Agent call or prior).
    2. If the file has entries, run the audit skill manually: read the last 5 lines of token-log.jsonl, calculate total estimated tokens, and print a formatted summary to verify the skill logic produces output (no need to write TOKEN-AUDIT.md — just confirm it runs).
    3. If token-log.jsonl is still empty (hook not yet fired in this session), write a synthetic test entry directly to confirm the audit skill can parse it:
       `{"ts":"2026-04-08T00:00:00Z","tool":"Agent","model":"sonnet","description":"test","est_input":100,"est_output":50,"total":150}`
       Then re-run the audit check against that entry.

    Document the smoke test result (pass/fail + entry count) in the task output.
  </action>
  <verify>
    <automated>grep -c "@file:" super-gsd/skills/gsd-orchestrate/SKILL.md || echo "@file: guard count: check manually (file may be new)"</automated>
    <automated>node -e "const lines=require('fs').readFileSync('.planning/metrics/token-log.jsonl','utf8').trim().split('\n').filter(Boolean); if(!lines.length){console.log('WARNING: token-log empty — write synthetic entry');process.exit(1);} const last=JSON.parse(lines[lines.length-1]); console.log('token-log last entry model:',last.model,'total:',last.total,'— ORCH-08: OK');" 2>&amp;1</automated>
  </verify>
  <done>gsd-orchestrate/SKILL.md contains @file: IPC guard after every gsd-tools Bash capture. token-log.jsonl has at least 1 parseable JSONL entry confirming ORCH-08 end-to-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gsd-tools Bash output → orchestrate skill | Large JSON payloads >50KB become @file: path strings — unguarded parse fails silently |
| SUPER-GSD patch → GSD 1.0 files | Additive patches to shared library files — bad syntax or duplicate application breaks gsd-tools globally |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-01 | Tampering | model-profiles.cjs double-patch | mitigate | Idempotency check: grep for SUPER-GSD-START before patching; skip if found |
| T-01-02-02 | Tampering | config.cjs routing override | mitigate | Override reads .planning/config.json with try/catch — falls through to GSD 1.0 logic on any error |
| T-01-02-03 | Denial of Service | @file: temp file left on disk | accept | Temp files in os.tmpdir() — OS cleans on reboot; no sensitive data in payload |
| T-01-02-04 | Information Disclosure | token-log.jsonl smoke test | mitigate | Synthetic test entry contains no secrets: model/description/counts only. No API keys, no user content. |
| T-01-02-05 | Repudiation | GSD 1.0 patch attribution | mitigate | SUPER-GSD-START/END markers document origin; comment "managed by install.sh" — clear attribution |
</threat_model>

<verification>
Run after both tasks complete:

1. `grep -q "SUPER-GSD-START" ~/.claude/get-shit-done/bin/lib/model-profiles.cjs && echo ok` — must print ok
2. `node -e "require('C:/Users/jack.berrow/.claude/get-shit-done/bin/lib/model-profiles.cjs'); console.log('ok')"` — must print ok (no syntax error)
3. `grep -c "@file:" super-gsd/skills/gsd-orchestrate/SKILL.md` — must be >= 1
4. `node -e "JSON.parse(require('fs').readFileSync('.planning/metrics/token-log.jsonl','utf8').trim().split('\n').pop()); console.log('valid jsonl: ok')"` — must print ok
5. Full phase success criteria (from ROADMAP.md): all 5 ROADMAP success criteria provably true
</verification>

<success_criteria>
- 3 GSD 1.0 files patched with SUPER-GSD-START/END markers, idempotent
- model-profiles.cjs syntax-valid with Node.js require()
- gsd-orchestrate SKILL.md has @file: guard at every gsd-tools Bash capture point
- token-log.jsonl has at least 1 JSONL entry parseable by the audit skill
- Running /gsd-token-audit --quick produces output (model, role, total tokens) without error
</success_criteria>

<output>
After completion, create `.planning/phases/01-token-foundation/01-02-SUMMARY.md` with:
FILES_CHANGED | VERIFICATION | DEVIATIONS | BLOCKERS | ONE_LINER
Max 300 words. Structured sections only. No preamble.
</output>
