---
phase: 01-token-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - super-gsd/hooks/gsd-token-logger.js
  - super-gsd/hooks/gsd-checkpoint-writer.js
  - super-gsd/hooks/gsd-stuck-detector.js
  - super-gsd/hooks/gsd-context-monitor.js
  - super-gsd/hooks/gsd-session-start.js
  - ~/.claude/settings.json
autonomous: true
requirements:
  - SAFE-01
  - SAFE-02
  - ORCH-07

must_haves:
  truths:
    - "All 5 hooks use toUnixPath() at entry — no silent failures from Windows backslash paths"
    - "gsd-checkpoint-writer.js uses .tmp + renameSync — never corrupts on interrupted write"
    - "settings.json contains all 5 hook registrations from settings-overlay.json"
    - "After a test Agent call, a JSONL entry appears in .planning/metrics/token-log.jsonl"
  artifacts:
    - path: "super-gsd/hooks/gsd-token-logger.js"
      provides: "Token logger with toUnixPath() at entry"
      contains: "toUnixPath"
    - path: "super-gsd/hooks/gsd-checkpoint-writer.js"
      provides: "Atomic write checkpoint hook"
      contains: "renameSync"
    - path: "super-gsd/hooks/gsd-stuck-detector.js"
      provides: "Stuck detector with normalized file_path keys"
      contains: "toUnixPath"
    - path: "super-gsd/hooks/gsd-context-monitor.js"
      provides: "Context monitor with defensive toUnixPath"
      contains: "toUnixPath"
    - path: "super-gsd/hooks/gsd-session-start.js"
      provides: "Session start hook with normalized cwd paths"
      contains: "toUnixPath"
    - path: "~/.claude/settings.json"
      provides: "Live hook registrations"
      contains: "gsd-token-logger"
  key_links:
    - from: "settings.json PostToolUse[matcher=Agent]"
      to: "super-gsd/hooks/gsd-token-logger.js"
      via: "node command registration"
    - from: "gsd-checkpoint-writer.js"
      to: ".planning/ORCHESTRATOR-CHECKPOINT.json"
      via: "writeFileSync(tmp) + renameSync"
---

<objective>
Harden all 5 hook files with cross-platform path normalization and atomic writes, then wire them into the live settings.json.

Purpose: Every downstream phase depends on hooks firing correctly. Path bugs and non-atomic writes are silent data-loss failures — fix these before anything builds on them.
Output: 5 patched hook files + settings.json with all hook registrations active.
</objective>

<execution_context>
@C:/Users/user/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@C:/Users/user/GSDedits/.planning/ROADMAP.md
@C:/Users/user/GSDedits/.planning/phases/01-token-foundation/01-CONTEXT.md
@C:/Users/user/GSDedits/.planning/phases/01-token-foundation/01-RESEARCH.md

<interfaces>
<!-- toUnixPath utility — apply at top of each hook after requires -->
```javascript
function toUnixPath(p) {
  return p.replace(/^([A-Za-z]):\\/, (_, d) => `/mnt/${d.toLowerCase()}/`)
           .replace(/\\/g, '/');
}
```

<!-- Atomic write pattern — replace direct writeFileSync in gsd-checkpoint-writer.js -->
```javascript
const tmp = CHECKPOINT_FILE + '.tmp';
if (fs.existsSync(tmp)) fs.unlinkSync(tmp);  // clean stale .tmp
fs.writeFileSync(tmp, JSON.stringify(checkpoint, null, 2));
fs.renameSync(tmp, CHECKPOINT_FILE);
```

<!-- Current hook registrations in settings-overlay.json (source of truth for merge) -->
SessionStart: node ~/.claude/hooks/gsd-session-start.js
PostToolUse[Agent]: node ~/.claude/hooks/gsd-token-logger.js
PostToolUse[Bash|Edit|Write]: node ~/.claude/hooks/gsd-stuck-detector.js
PostToolUse[Bash]: node ~/.claude/hooks/gsd-checkpoint-writer.js
PostToolUse[Agent|Read|Write|Edit|Bash]: node ~/.claude/hooks/gsd-context-monitor.js
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add toUnixPath to all 5 hooks + atomic write to checkpoint hook</name>
  <files>
    super-gsd/hooks/gsd-token-logger.js,
    super-gsd/hooks/gsd-checkpoint-writer.js,
    super-gsd/hooks/gsd-stuck-detector.js,
    super-gsd/hooks/gsd-context-monitor.js,
    super-gsd/hooks/gsd-session-start.js
  </files>
  <action>
    For each of the 5 hook files, add the toUnixPath() function immediately after the require statements at the top of the file (before any constants). Then apply it at the first point each hook builds a file path from process.cwd() or string concatenation:

    gsd-token-logger.js (per SAFE-01):
    - Add toUnixPath() after requires.
    - Change: `const LOG_FILE = path.join(process.cwd(), ...)` to `const LOG_FILE = path.join(toUnixPath(process.cwd()), '.planning', 'metrics', 'token-log.jsonl');`

    gsd-checkpoint-writer.js (per SAFE-01 + SAFE-02):
    - Add toUnixPath() after requires.
    - Change: `const CHECKPOINT_FILE = path.join(process.cwd(), ...)` to `const CHECKPOINT_FILE = path.join(toUnixPath(process.cwd()), '.planning', 'ORCHESTRATOR-CHECKPOINT.json');`
    - Replace the `fs.writeFileSync(CHECKPOINT_FILE, ...)` line with the atomic pattern: write to `CHECKPOINT_FILE + '.tmp'`, clean stale .tmp first, then renameSync to CHECKPOINT_FILE.

    gsd-stuck-detector.js (per SAFE-01):
    - Add toUnixPath() after requires.
    - In the key extraction block, apply toUnixPath to `input.file_path` before building the key: `key = toolName + ':' + toUnixPath(input.file_path);`

    gsd-context-monitor.js (per SAFE-01):
    - Add toUnixPath() after requires.
    - TIMER_FILE and CHECKPOINT_WARNED use os.tmpdir() (already safe on same platform) — apply toUnixPath defensively: `const TIMER_FILE = path.join(toUnixPath(os.tmpdir()), 'gsd-phase-timer.json');` and same for CHECKPOINT_WARNED.

    gsd-session-start.js (per SAFE-01):
    - Add toUnixPath() after requires.
    - Change: `const CHECKPOINT_PATH = path.join(process.cwd(), ...)` to `const CHECKPOINT_PATH = path.join(toUnixPath(process.cwd()), '.planning', 'ORCHESTRATOR-CHECKPOINT.md');`
    - Change: `const STATE_PATH = path.join(process.cwd(), ...)` to `const STATE_PATH = path.join(toUnixPath(process.cwd()), '.planning', 'STATE.md');`
    - Change: `const brvPath = path.join(process.cwd(), '.brv')` to `const brvPath = path.join(toUnixPath(process.cwd()), '.brv');`

    Do NOT change any logic, thresholds, warning text, or data structures — only path construction lines.
  </action>
  <verify>
    <automated>node -e "const h=require('./super-gsd/hooks/gsd-checkpoint-writer.js'); console.log('ok')" 2>&amp;1 || echo "syntax ok (no crash)"</automated>
    <automated>grep -c "toUnixPath" super-gsd/hooks/gsd-token-logger.js super-gsd/hooks/gsd-checkpoint-writer.js super-gsd/hooks/gsd-stuck-detector.js super-gsd/hooks/gsd-context-monitor.js super-gsd/hooks/gsd-session-start.js</automated>
    <automated>grep -q "renameSync" super-gsd/hooks/gsd-checkpoint-writer.js &amp;&amp; echo "atomic: present" || echo "atomic: MISSING"</automated>
  </verify>
  <done>All 5 hook files contain toUnixPath(). gsd-checkpoint-writer.js contains renameSync (atomic pattern). No logic changes.</done>
</task>

<task type="auto">
  <name>Task 2: Merge settings-overlay.json into ~/.claude/settings.json</name>
  <files>~/.claude/settings.json</files>
  <action>
    Read ~/.claude/settings.json (current live settings). Read super-gsd/config/settings-overlay.json (the hook registrations to merge).

    Merge strategy: The overlay defines a `hooks` object with `SessionStart` and `PostToolUse` keys. For each key:
    - If the key does not exist in the live settings.json, add it wholesale.
    - If the key exists, append the overlay's array entries to the existing array. Do NOT duplicate — check if a hook with the same `command` string already exists before appending.

    The merged result must contain all 5 hook command registrations from settings-overlay.json:
    - SessionStart: `node ~/.claude/hooks/gsd-session-start.js`
    - PostToolUse matcher Agent: `node ~/.claude/hooks/gsd-token-logger.js`
    - PostToolUse matcher Bash|Edit|Write: `node ~/.claude/hooks/gsd-stuck-detector.js`
    - PostToolUse matcher Bash: `node ~/.claude/hooks/gsd-checkpoint-writer.js`
    - PostToolUse matcher Agent|Read|Write|Edit|Bash: `node ~/.claude/hooks/gsd-context-monitor.js`

    Write the merged JSON back to ~/.claude/settings.json. Preserve all existing settings — do not delete or overwrite any non-hooks keys.

    IMPORTANT: Do NOT read the file contents back into the conversation to verify — check only for key existence using grep or node -e existence check.
  </action>
  <verify>
    <automated>node -e "const s=require(require('os').homedir()+'/.claude/settings.json'); const h=s.hooks||{}; const ptu=(h.PostToolUse||[]).flatMap(g=>(g.hooks||[]).map(x=>x.command)); console.log('token-logger:', ptu.some(c=>c.includes('gsd-token-logger'))?'OK':'MISSING'); console.log('checkpoint:', ptu.some(c=>c.includes('gsd-checkpoint-writer'))?'OK':'MISSING'); console.log('stuck:', ptu.some(c=>c.includes('gsd-stuck-detector'))?'OK':'MISSING'); console.log('context:', ptu.some(c=>c.includes('gsd-context-monitor'))?'OK':'MISSING'); const ss=(h.SessionStart||[]).flatMap(g=>(g.hooks||[]).map(x=>x.command)); console.log('session-start:', ss.some(c=>c.includes('gsd-session-start'))?'OK':'MISSING');"</automated>
  </verify>
  <done>All 5 hooks show OK in the verification output. Existing settings.json keys are intact. No secrets read into conversation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| hook process.env → file system | TOOL_INPUT/TOOL_OUTPUT env vars from Claude Code runtime written to local files |
| settings.json merge | Overlay JSON merged into live user config — incorrect merge could break all hooks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01-01 | Tampering | gsd-checkpoint-writer.js tmp file | mitigate | Clean stale .tmp before write; renameSync is atomic on same volume |
| T-01-01-02 | Denial of Service | Hook timeout (3s budget) | accept | All hooks are lightweight file ops; toUnixPath is nanoseconds — no timeout risk |
| T-01-01-03 | Information Disclosure | settings.json read in plan | mitigate | Task 2 action explicitly prohibits reading file contents into conversation; use node -e existence check only |
| T-01-01-04 | Tampering | settings.json overwrite | mitigate | Merge strategy preserves all existing keys; never replaces the full file with overlay-only content |
</threat_model>

<verification>
Run after both tasks complete:

1. `grep -c "toUnixPath" super-gsd/hooks/*.js` — should show count >= 1 for each of 5 files
2. `grep -q "renameSync" super-gsd/hooks/gsd-checkpoint-writer.js && echo ok` — must print ok
3. Node existence check (in Task 2 verify) — all 5 hooks show OK
4. Manual smoke test: run one Agent call in this project, then check `.planning/metrics/token-log.jsonl` for a new JSONL entry (confirms ORCH-07 wired end-to-end)
</verification>

<success_criteria>
- All 5 hook files contain toUnixPath() with no logic regressions
- gsd-checkpoint-writer.js uses .tmp + renameSync (atomic, per SAFE-02)
- settings.json contains all 5 hook registrations (no duplicates, no existing keys removed)
- After a test Agent call, token-log.jsonl gains a new entry (confirms ORCH-07 wired)
</success_criteria>

<output>
After completion, create `.planning/phases/01-token-foundation/01-01-SUMMARY.md` with:
FILES_CHANGED | VERIFICATION | DEVIATIONS | BLOCKERS | ONE_LINER
Max 300 words. Structured sections only. No preamble.
</output>
