---
status: DRAFT_DEPENDENCY_PENDING
date: 2026-09-09
scope: SOURCE_ONLY_EIGHT_PRODUCER_FAMILIES
dependency: P170_GENUINE_LINUX_ACCEPTANCE_AND_EXISTING_PHASE_CLOSE
source_head: 4934ccb72e0f88a5caa58086c3f5290e5317bdef
p171_activated: false
---

# P171 adapter seam map

Read-only preparation for 170-08 Task 3, not an implementation plan or gate verdict. No live ledger scans, producer/gate execution, tests, or provider calls occurred. The inspected producer/intake files had no worktree changes against the source HEAD above; concurrent 170-09 launcher/test repairs are separate. The earlier `2026-09-09-atlas-operational-coverage-census.md` describes revision `935962c`; its historical declaration counts are not refreshed live counts here. Eight families below count deterministic and qualitative MUDA separately.

The governing design puts ledger normalization, explicit correlation and invocation census in P171 (`2026-09-03-sgsd-token-economics-atlas-design.md:238`, `:253`, `:547`, `:833`). Existing gates retain verdict, eligibility, execution and exit-policy authority. These adapters must only observe.

## Current intake boundary

- Reuse existing canonical validation/canonicalization (`super-gsd/tools/telemetry-atlas/contract.cjs:43`, `:64`, `:71`). Version 1 has gate/finding/repair/outcome/coverage events and nullable identity fields, but permits only enumerated keys, bounded atom strings, nonnegative finite numbers and events at most 32 KiB. It has no general metadata object or numeric MUDA-probe/threshold array. Raw paths, free text, arbitrary nested producer rows and provenance-offset objects cannot simply be copied into it.
- Existing registered HTTP `/v1/events` already accepts non-native canonical events (`server.cjs:132`, `:178`). A new endpoint is not inherently needed. Native `codex_rollout` stays private-spool-only; the current spool admits only native rollout, status-line and lifecycle sources (`server.cjs:269`). Do not relabel ledger events as those sources or widen native intake as an adapter shortcut.
- For non-native events, `global-store.cjs:74` overwrites run/project/role attribution with the receiving registration. A historical import must not borrow today's launcher and claim historical invocation attribution. Use an independently matched historical registration where available; otherwise retain uncorrelated evidence/exclusion and an explicit gap, not a fabricated `sgsd_run_id`. Project directory identity is local registration identity, not proof of the repository edited (`global-store.cjs:25`).
- Native envelopes expressly forbid extra operation identities, expanded scope and gate/outcome sections (`accounting.cjs:12`). Preserve them unchanged. Semantic records can link separately through observed IDs; `classifyAccounting` only counts recognized native request/response sources (`:56`). Ledger estimates and semantic events must never add native tokens a second time. Keep Codex `request_id:null`; exact response usage is not exact HTTP-request identity.

## Eight concrete seams

All short source paths in the table are under `super-gsd/`.

| Family / existing source | Observable identity and safe projection | Missing join / privacy boundary |
| --- | --- | --- |
| Worker attempt: `tools/codex-worker/run.cjs:55`, `mailbox.cjs:75`; `scripts/lib/codex-worker-shell.sh:183`, `:212`. Source files: `.planning/worker-sessions/{worker_id}/state.json` and immutable `wrapper-result.json`. | `worker_id`, `instance`, `wrapper_attempt_id`, `atlas_run_id`, acknowledged `thread_id`/`turn_id`, `resumed_from`, phase/plan/step, model/effort, status; wrapper exit plus report digest/size. Join receipt to exactly one worker/attempt and native rows through registration + run/thread/turn. Reuse `usage.projectUsageRecord` for retained raw token records (`tools/codex-worker/usage.cjs:11`). | Mutable state is not an event history. Core `completed` is not wrapper/report success; receipt absence is unknown. Resume shares a thread but creates a new worker/attempt. No required task/handoff/gate-invocation/finding/repair ID. Drop `pending`, commands/replies, raw failure text, owner/project/report paths; retain checked aliases, IDs and bounded error codes only. |
| Gate value: `scripts/lib/gate-value-log.cjs:191`, `:271`; `.planning/metrics/gate-value-log.jsonl`. | Envelope-v1 `run_id`, timestamp, gate, phase/milestone, outcome/status, duration, small registry snapshot. Writer returns the actual appended normalized row or false. | `run_id` is an envelope record ID, not `sgsd_run_id` or an enforced shared gate invocation. Normalization drops unspecified extensions; adding a caller field alone will lose it. No plan/worker/provider-response/finding/repair join. Allowlist reason codes; do not copy arbitrary evidence refs, next-action text or paths. |
| Review ledger: `scripts/lib/review-ledger.cjs:105`, `:197`; `.planning/metrics/review-ledger.jsonl`, with per-phase `commit-reviews.jsonl` provenance. | Envelope ID/status/time/duration and explicit phase/milestone/source tags; versioned projection of known `_legacy` plan/tier/provider/count fields and report hash/ref. | `_legacy` preserves arbitrary input verbatim. Existing aggregation dedups a legacy tuple, not unique findings or provider requests (`:273`, `:337`). No required gate invocation/attempt/finding/repair IDs. Do not import report content, finding prose, `_legacy` wholesale, or call the mutating aggregate operation merely to read evidence. |
| Deterministic MUDA: `scripts/sgsd-muda-audit.sh:153`, `:518`; `sgsd-muda-probe.sh:260`. Sources: `muda-log.jsonl`, retained `WASTE.md` probe JSON. | Ledger timestamp/phase/warn/fail/exit and five named probe verdicts. The retained probe JSON additionally contains numeric values, threshold descriptions, waste class and evidence. | No run/invocation/worker ID; ledger loses numeric values, input denominator and richer evidence. `WASTE.md` is rewritten, so bind retained revision/digest. No-input extra-processing defaults to PASS (`sgsd-muda-probe.sh:149`); preserve that producer verdict while coverage is `no_coverage`. Debug `--probe codex` inserts synthetic mechanical PASS rows (`sgsd-muda-audit.sh:156`); never count them as executed probes. Never copy raw project/evidence strings. Numeric probe payload needs a typed, content-free referenced observation artifact or an explicitly planned narrow schema addition, not misuse of token fields. |
| Qualitative MUDA: `scripts/sgsd-muda-audit.sh:375`, `:407`, `:529`; same WASTE/ledger plus normal wrapper evidence. | Actual wrapper phase and step `muda-qualitative`; ledger optional `probes.codex_qualitative_waste` verdict. If retained worker/receipt IDs exist, use them for the separate model operation and native usage join. | Actual script predicate requires mechanical exit 0; do not substitute the registry's broader declaration or reevaluate the gate in Atlas. CLI call supplies no explicit project/plan/owner here, so verify actual worker scope rather than infer it from WASTE. Shared WASTE and timestamp do not provide exact causal join. Drop prompt/diff/report prose; missing optional verdict does not prove why it was skipped. |
| Gate evidence / scheduled routes: `scripts/lib/gate-evidence-log.cjs:79`, `:170`; `orchestrator-hooks.cjs:1064`, `:676`. Source: `.planning/metrics/gate-evidence.jsonl`. | Envelope ID; allowlisted signal/event/route/skill/moment/mode/decision, gate ref where actually present, dry-run, trigger measurements, exit/meaning/duration. Actual decision versus actual execution remain separate observations. Existing `_appendGateValueOutcome` validates producer eligibility (`orchestrator-hooks.cjs:713`). | Extension fields are arbitrary; exclude dispatch argv/cwd, excerpts, reason/next-action free text. Decision, execution and gate-value writes generate separate IDs; `parent_decision:'fired'` is a label, not a parent record ID. `route_id` repeats across invocations. Current execution row does not carry the decision's run ID, gate_ref or dry_run: do not manufacture exact joins or infer missing as false. |
| Edge guard: `scripts/lib/edge-guard.cjs:56`, `:95`; `.planning/metrics/edge-guard-log.jsonl`. | Timestamp, phase/plan, from/to step, gate, expected/actual/missing emit membership and existing resolution. Safe projection may use validated artifact aliases/digests and counts. | No stable event/run/gate-invocation ID; `context` is arbitrary. Empty expected emits are not a complete gate denominator. Step 11 returns before emitting (`:68`). Preserve existing resolution; do not introduce a new checker or infer all missing gates from absent files. |
| Orchestrator live: `scripts/lib/orchestrator-live-writer.cjs:43`, `:71`; `.planning/ORCHESTRATOR-LIVE.jsonl`. | Schema-v1 timestamp, one of 16 event types and milestone/phase/plan. Project-specific, type-specific allowlist for any actually supplied ID/count fields in `data`. | No required stream sequence, run/session/attempt ID or typed `data` schema. `run_started` is an event name, not proof of an attached Atlas run. Do not inspect free text to guess operation scope or transport arbitrary `data`, progress, prompts or attention messages. |

Record identity is not causal identity. A digest/reference to retained source bytes and location can make import/replay auditable; it does not create a missing gate invocation or finding ID. Timestamp/order matches are at most inferred (`design:270`), never exact token attribution. Ambiguous joins remain separate observations with an explicit reason.

## Smallest prospective adapter/test slices

These are bounded candidates for root's eventual approved plan, not authorization to modify any named file.

1. **Pure projection and bounded reader.** New narrowly scoped `super-gsd/tools/telemetry-atlas/sgsd-ledger.cjs` and `sgsd-ledger.test.cjs` could own only the eight versioned source projections, deterministic source-record provenance and explicit unknown/exclusion results. Reuse existing contract validation and contained-file utilities. No gate execution, source-ledger rewriting or historical `_legacy` aggregate rebuild. Tests: nested privacy canaries, malformed rows/tails, unsupported producer versions, byte/row caps, missing files, rotation/rewrite, duplicate replay/conflict, and same timestamp/phase across projects. Gate-value/review readers silently discard malformed rows; they cannot alone prove a completeness denominator. Gate-evidence exposes skipped-line metadata (`gate-evidence-log.cjs:216`), which must not be lost through array JSON serialization.
2. **Worker-first correlation and delivery.** Keep `usage.cjs`/`accounting.cjs` authoritative and unchanged unless separately justified. Implement explicit worker/receipt/registration/native joins in that adapter and use the existing bounded registered events route; receiver/global-store tests should prove no acceptance under a foreign or borrowed run and no additive semantic tokens. Test completed worker plus failed/missing wrapper receipt, resumed thread/new turn, duplicate raw response, cross-project collisions, stale registration and unavailable usage. If a private semantic spool is chosen instead, its typed admission is a separate explicit task, not reuse of native-source credentials.
3. **Add missing causal fields only at real producers.** After projection identifies gaps, a separate exact-file task can add an optional shared operation ID at `orchestrator-hooks.cjs` decision/execution/gate-value boundaries and narrowly preserve it in `gate-value-log.cjs`; similarly link an actual review's wrapper receipt through `review-ledger.cjs`. Tests must show original gate predicates, return values, exit policy and native accounting unchanged, and repeated/concurrent routes remain distinct. Do not infer successful source appends from an attempted API call returning false. Broader finding/repair lineage requires actual structured identifiers at dispatch/recheck boundaries, not synthesized IDs from report counts; scope that later from the design's fingerprint contract (`design:558`).
4. **MUDA/live/edge observations without enforcement changes.** Use the same projection module, retained typed probe data and exact source provenance first. Add producer identity/coverage retention only where the eventual plan names the responsible files and preserves script verdicts/side effects. Zero-provider tests cover mechanical versus qualitative separation, synthetic/debug probe rows, absent input versus zero measured defects, overwritten WASTE, repeated edge transitions, missing route execution identity and arbitrary live `data`. Do not rerun probes to reconstruct historical evidence or claim curation means reinjection/prevention.

P170 closure remains a prerequisite. No current producer scan proves deployed coverage. P171 exit still needs installed capture configuration, cross-repo/gate-repair evidence and the prescribed 24-hour calibration denominators; only then can the frozen comparison clock begin (`design:833`). Windows and fleet coverage remain separate open boundaries.
