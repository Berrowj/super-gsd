# Milestone v1.3: Multimodal Review — Codex Integration

**Status:** ✅ SHIPPED 2026-04-24
**Phases:** 14, 15, 16
**Total Plans:** 12 plans (4 Phase 14 + 5 Phase 15 + 3 Phase 16)
**Total Commits:** 66 (feat/docs/fix/chore across 3 phases)
**Scope:** 41 files changed, 5,414 insertions, 177 deletions
**Tag:** `v1.3`

## Overview

v1.3 delivers a cross-vendor reviewer lane for code-review-shaped work in SGSD — the first break from the mono-vendor (all-Claude) posture that held through v1.1/v1.2. Codex CLI (OpenAI) is now a first-class reviewer for three gate surfaces (per-dispatch ATC, phase-level ATC, adversarial verifier challenger) and a fourth qualitative MUDA probe. The milestone also establishes VTP knowledge-substrate enrichment as a cross-phase primitive feeding research/planning/pattern-mapping/assumptions dispatches.

Per original BRIEF.md framing, the milestone is explicitly **not** "add Codex everywhere" — classifier stays Haiku, mechanical verifier stays deterministic, executors stay Sonnet. Only review-shaped surfaces where disagreement is useful got the cross-vendor treatment. Kill condition at milestone close (CODEX-12) is the lifecycle enforcement — if Codex doesn't earn its keep quantifiably (critical-count-delta ≥ 5 + claude-tokens-saved ≥ 50k), `codex_enabled` auto-flips back to `false`.

**Historic artifact:** First live Codex invocation captured 2026-04-24T02:39:05Z during the MUDA qualitative probe run — `.planning/metrics/codex-log.jsonl` + `codex-live.json` prove the shell-dispatch pipe is operational end-to-end. (Timed out at 60s on a 35KB prompt; tuning carries as tech debt.)

## Phases

### Phase 14: Codex CLI Provider Substrate

**Goal:** Ship the reviewer-provider abstraction dark — `codex-exec.sh` wrapper, `review-providers.yaml` registry, `providers-registry.cjs` dispatch indirection, `gates.yaml` `reviewer_provider:` field, contract-check harness. No live Codex routing yet (`codex_enabled: false` at close).
**Depends on:** Phase 16 (per BRIEF D-01 — inherits VTP-grounded context)
**Plans:** 4 plans

- [x] 14-01: codex-exec.sh wrapper (Claude/Codex contract parity)
- [x] 14-02: review-providers.yaml + providers-registry.cjs + 2 agent stubs + gates.yaml schema extension
- [x] 14-03: config.json review_providers block + known-keys patch
- [x] 14-04: contract-check.mjs pure-parser harness + fixtures + verify.mjs

**Verification:** PASS 6/6 V-predicates, verify.mjs exits 0 with 6/6 D-23 invariants green.
**ATC-REVIEW:** PASS-WITH-WARNINGS (0 critical, 5 warnings routed forward as Phase 15 entry conditions — all addressed).

### Phase 15: Codex-Routed Gates + Qualitative MUDA Probe

**Goal:** Flip review-shaped ATC gates to Codex by registry, add cross-vendor adversarial challenger, land first qualitative overproduction MUDA probe, and ship milestone-close kill check.
**Depends on:** Phase 14 (substrate required)
**Plans:** 5 plans in 4 serialized waves (SKILL.md touches serialize)

- [x] 15-01: Provider-indirection-wire (CODEX-07 — Wave 2 switch flip; includes W-1 fix + W-2 registry_version bump + W-3 plan typo + W-4 --phase numeric guard)
- [x] 15-02: Qualitative MUDA probe (CODEX-08 + CODEX-09 — Wave 1 sgsd-muda-audit.sh 4th probe + qualitative-waste-audit gate row)
- [x] 15-03: Quota-offload metric (CODEX-10 — Wave 1 token-log.jsonl provider field + Multimodal Review Offload dashboard tile)
- [x] 15-04: Cross-vendor adversarial (CODEX-11 — Wave 3 Step 9.6 MACH-04 challenger always non-primary vendor)
- [x] 15-05: Kill-condition milestone wire (CODEX-12 — Wave 4 sgsd-token-audit --milestone-close-check + sgsd-complete-milestone step renumber + Phase 15 verify.mjs)

**Verification:** PASS 6/6 must-haves, verify.mjs exits 0 with 9/9 invariants green.
**ATC-REVIEW:** PASS-WITH-WARNINGS (0 critical, 4 warnings — 2 hotfixed in-phase (WR-03 invocation_type typo, WR-04 COMMITS_IN_PHASE default), 2 carry-over from Phase 14 tech debt).

### Phase 16: VTP Enrichment as Cross-Phase Primitive

**Goal:** Make VTP evidence a first-class input to every SGSD planning/research/triage surface. `sgsd-triage` Step 0 VTP-router, `gsd-phase-researcher` + `gsd-planner` + `gsd-codebase-mapper` + `gsd-assumptions-analyzer` agent patches, shared `vtp-context-composer.cjs` helper, `vtp-routing-log.jsonl` telemetry, `vtp_advise_service_enrichment` integration.
**Depends on:** VTP upstream (MCP server operational + `resolveProjectRoot` fix landed 2026-04-23)
**Plans:** 3 plans (1 per wave)

- [x] 16-01: Wave A primitive (VTP-01, VTP-04, VTP-05, VTP-09, VTP-10) — composer + triage Step 0 + config toggle + smoke
- [x] 16-02: Wave B agent patches (VTP-02, VTP-03, VTP-06, VTP-07) — frontmatter + body patches for 4 agents
- [x] 16-03: Wave C advise (VTP-08a, VTP-08b) — /sgsd-vtp-advise standalone skill + sepl major-detection + advise-enrich

**Verification:** PASS 6/6 V-predicates (retroactive 2026-04-24, closed v1.3 audit gap). 10 commits verified across 3 waves.

---

## Milestone Summary

### Key Decisions

- **D-01 (BRIEF):** Substrate-first sequencing — Phase 14 dark + Phase 15 live. Validated: clean handoff, zero architectural surprises at Phase 15.
- **D-25 (Phase 15):** Wave 1 dual SKILL.md touch allowed (Steps 9.2 vs 11 non-overlapping). Validated: no parallel clobber observed empirically.
- **D-17a (Phase 15):** Adversarial challenger skip-on-unavailable with no same-vendor fallback. Logged as `VERIFIER_ADVERSARIAL_SKIP`.
- **D-13a (Phase 15):** Token offload is an estimated metric, not a billing audit. Disclaimer in dashboard tile.
- **VTP routing tool layer separation:** `vtp_search_substrate` for content (books, papers, transcripts), `wiki_search` for meta (people/projects/ideas/analyses). Captured as `feedback_vtp_search_layer_routing.md` memory entry.

### Issues Resolved

- Mono-vendor SGSD architecture → cross-vendor reviewer lane (3 ATC gates + 1 MUDA probe + 1 adversarial path)
- Claude Max OAuth quota burn on review-shaped dispatches → Codex quota offload path operational
- Same-vendor adversarial challenger blind spots → true cross-vendor diversity at Step 9.6
- MUDA taxonomy's "operator-judgment-only" overproduction class → first qualitative probe lands
- VTP evidence silent-bypass regression risk → structural injection contract enforced, `<vtp_evidence>` header in every planning dispatch
- Stale-MCP child process diagnostic pattern → `feedback_stale_mcp_process_diagnosis.md` memory entry

### Technical Debt Incurred (carried to v1.4)

- **codex_timeout_seconds tuning** — 60s insufficient for 35KB qualitative analysis prompts; raise to 120-180s for analysis workloads
- **providers-registry.cjs WR-01** — JSDoc drift (stale `reviewer_agent` shape-discriminator reference at lines 17-19, 137-138)
- **providers-registry.cjs WR-02** — dead-code branch at lines 157-158 (unreachable after W-1 predicate narrowing)
- **sgsd-muda-audit.sh WASTE.md display inconsistency** — summary table shows 3 mechanical probes, raw JSON has 5 with inventory FAIL not surfaced in summary text
- **Missing 15-01-SUMMARY.md and 15-03-SUMMARY.md** — pattern is one summary per plan; 3/5 plans have summaries, 2 absent
- **P5 Codex Monitor artifact gap** — commit c8b2d25 (648 LOC, 4 files) landed without `.planning/` artifact; backfill a retroactive SPEC/PLAN
- **REQUIREMENTS.md staleness** — still scopes v1.2; CODEX-01..12 + VTP-01..10 REQ-IDs live only in BRIEF.md + per-plan SUMMARY frontmatter. Fresh v1.4 requirements at `/gsd-new-milestone v1.4`.

### Issues Deferred (non-urgent)

- Third provider support (Gemini, local models) — registry supports shape, first-real-data-first
- Per-project provider overrides — deferred post-v1.3
- Codex on classifier (Haiku) or primary verifier (mechanical) — explicitly scope-disciplined as NEVER

### First Live Codex Invocation (historic artifact)

```
Timestamp: 2026-04-24T02:39:05Z
Step: muda-qualitative
Provider: codex-cli-reviewer
Invocation: shell
Model: gpt-5.4 (via codex exec --sandbox read-only --ephemeral)
Prompt: 35004 bytes
Outcome: exit 5 (timeout at 60s)
Pipe status: operational end-to-end
Evidence: .planning/metrics/codex-log.jsonl + codex-live.json
```

The config → resolveReviewerProvider → shellDispatch → codex-exec.sh → `codex exec` pipe works end-to-end. Timeout is tuning, not wiring.

---

_For current project status, see `.planning/ROADMAP.md`_
