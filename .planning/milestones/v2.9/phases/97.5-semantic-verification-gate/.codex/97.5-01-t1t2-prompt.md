# SDD Implementer Task — P97.5 Plan 97.5-01 (t1 + t2)

You are a fresh SDD implementer working in the SGSD repository at the workspace root. You have **no inherited context**. Read only what this prompt names.

## What you are doing

Add a new required field `semantic_acceptance_criteria` to the SGSD v2 plan schema and prove it works with fixture tests. This is the load-bearing change of phase P97.5 and is driven by DLB-07.

## Read these files before writing

1. `.planning/milestones/v2.9/phases/97.5-semantic-verification-gate/97.5-CONTEXT.md` — phase scope
2. `.planning/milestones/v2.9/phases/97.5-semantic-verification-gate/97.5-01-schema-enforcement-PLAN.md` — your task contract (tasks t1 and t2)
3. `super-gsd/templates/plan-schema-v2.json` — current schema (DO NOT remove or rename existing keys)
4. `super-gsd/tools/plan-schema/validate.cjs` — to confirm `e.keyword === 'errorMessage'` path passes message through unchanged
5. `.planning/milestones/v1.2/phases/11-plan-schema-v2/fixtures/good-plan.md` — existing v2 fixture style; mimic this shape for the new fixtures

## Files you will create or modify

Exactly these five files. Do not touch anything else.

- `super-gsd/templates/plan-schema-v2.json` — modify
- `super-gsd/tools/plan-schema/fixtures/missing-semantic-ac.md` — create
- `super-gsd/tools/plan-schema/fixtures/empty-semantic-ac.md` — create
- `super-gsd/tools/plan-schema/fixtures/incomplete-semantic-ac.md` — create
- `super-gsd/tools/plan-schema/fixtures/good-semantic-ac.md` — create
- `super-gsd/tools/plan-schema/validate.test.cjs` — create

(That is six files; the schema is modify, the rest are create.)

## Schema changes (t1)

In `super-gsd/templates/plan-schema-v2.json`, make exactly these five additions:

1. Plan-level `required` array: append `"semantic_acceptance_criteria"` as a third entry. Final value: `["schema_version", "tasks", "semantic_acceptance_criteria"]`.

2. Plan-level `properties.semantic_acceptance_criteria`:
   ```json
   {
     "type": "array",
     "minItems": 1,
     "items": { "$ref": "#/definitions/semantic_ac" },
     "description": "Each entry: a falsifiable claim that a real-world input produces a specific outcome (DLB-07, SCHEMA-09)."
   }
   ```

3. Plan-level `errorMessage` block (sibling to `properties`, peer to the existing `additionalProperties`):
   ```json
   "errorMessage": {
     "required": {
       "semantic_acceptance_criteria": "plan must declare 'semantic_acceptance_criteria' array with >=1 entry (SCHEMA-09)"
     },
     "minItems": "plan 'semantic_acceptance_criteria' must contain >=1 entry (SCHEMA-09)"
   }
   ```

4. `definitions.semantic_ac`:
   ```json
   {
     "type": "object",
     "required": ["input", "expected_outcome", "verification_cmd"],
     "additionalProperties": true,
     "errorMessage": {
       "required": {
         "input": "semantic_acceptance_criterion must declare 'input' (SCHEMA-10)",
         "expected_outcome": "semantic_acceptance_criterion must declare 'expected_outcome' (SCHEMA-10)",
         "verification_cmd": "semantic_acceptance_criterion must declare 'verification_cmd' (SCHEMA-10)"
       }
     },
     "properties": {
       "input": { "type": "string", "description": "Description of the real-world input the verification command exercises." },
       "expected_outcome": { "type": "string", "description": "What the system must produce for the input to pass." },
       "verification_cmd": { "type": "string", "description": "Shell command that runs against real data and exits 0 iff expected_outcome holds." }
     }
   }
   ```

5. The resulting JSON must remain valid draft-07 and must compile with `ajv@^8.18.0` + `ajv-errors`.

Do not change any other key. Do not reformat keys you are not touching.

## Fixture files (t2)

All four fixtures are minimal valid v2 plans. They differ only in their `semantic_acceptance_criteria` block (or lack thereof). Use this template; substitute the SAC block per fixture:

```yaml
---
schema_version: 2
tasks:
  - id: t1
    agent: gsd-executor
    model: codex
    files_touched: [tmp.txt]
    input_contract: nothing
    output_contract: nothing
    hypothesis: nothing
    falsifier: nothing
    stop_rule: nothing
{SAC_BLOCK}
---

# Fixture
```

Per file:

- `missing-semantic-ac.md`: `{SAC_BLOCK}` is empty (no `semantic_acceptance_criteria` key at all)
- `empty-semantic-ac.md`: `{SAC_BLOCK}` = `semantic_acceptance_criteria: []`
- `incomplete-semantic-ac.md`: `{SAC_BLOCK}` = a one-entry array where `verification_cmd` is absent (input + expected_outcome present)
- `good-semantic-ac.md`: `{SAC_BLOCK}` = a one-entry array with all three keys populated with non-empty strings

## Test runner (t2)

`super-gsd/tools/plan-schema/validate.test.cjs`:

- CommonJS, uses only Node built-ins (`node:assert`, `node:child_process`, `node:path`, `node:fs`)
- AAA pattern: Arrange (compose validator command), Act (execFileSync), Assert (exit code + stderr substring)
- Five test cases:
  1. `missing-semantic-ac.md` → exit 1, stderr contains `SCHEMA-09`
  2. `empty-semantic-ac.md` → exit 1, stderr contains `SCHEMA-09`
  3. `incomplete-semantic-ac.md` → exit 1, stderr contains `SCHEMA-10`
  4. `good-semantic-ac.md` → exit 0, stderr contains `VALID`
  5. `97.5-01-schema-enforcement-PLAN.md` (the SDD plan itself, at `.planning/milestones/v2.9/phases/97.5-semantic-verification-gate/`) → exit 0, stderr contains `VALID`
- On all pass: print `5 passed, 0 failed` to stderr, exit 0
- On any fail: print which case failed with the actual exit code + stderr excerpt, exit 1
- No external test framework. No async. Synchronous execFileSync calls only.

## Verification you must run before declaring done

Run these commands in order. All must succeed:

```bash
node -e "JSON.parse(require('fs').readFileSync('super-gsd/templates/plan-schema-v2.json'))"
node super-gsd/tools/plan-schema/validate.test.cjs
```

The test runner is your only integration check. If any case fails, fix and re-run before reporting.

## Out of scope

- Do not modify `super-gsd/tools/plan-schema/validate.cjs`. The existing `errorMessage` path passes messages through. If the test runner fails because messages are not surfacing, debug the schema, not the validator.
- Do not modify any existing PLAN.md in `.planning/`.
- Do not run `npm install` unless `node super-gsd/tools/plan-schema/validate.cjs --help` fails with a missing-module error.
- Do not create a 97.5-BACKFILL.md. That is a separate task t3 not in this dispatch.

## Report format (write to your --report-out path)

```
FILES_CHANGED:
  super-gsd/templates/plan-schema-v2.json (modified)
  super-gsd/tools/plan-schema/fixtures/missing-semantic-ac.md (created)
  super-gsd/tools/plan-schema/fixtures/empty-semantic-ac.md (created)
  super-gsd/tools/plan-schema/fixtures/incomplete-semantic-ac.md (created)
  super-gsd/tools/plan-schema/fixtures/good-semantic-ac.md (created)
  super-gsd/tools/plan-schema/validate.test.cjs (created)

VERIFICATION:
  node -e "JSON.parse(...)" → exit 0 ✓
  node super-gsd/tools/plan-schema/validate.test.cjs → exit 0 ✓, "5 passed, 0 failed"

DEVIATIONS: <none or list>
BLOCKERS: <none or describe>
ONE_LINER: SCHEMA-09 and SCHEMA-10 enforce semantic_acceptance_criteria in plan schema v2; 5/5 fixture tests green.
```

Be terse. No prose beyond the report.
