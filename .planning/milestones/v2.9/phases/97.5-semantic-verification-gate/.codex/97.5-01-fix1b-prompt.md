# SDD Implementer — P97.5 Plan 97.5-01 follow-up fix #1 (retry)

You are a fresh SDD implementer. No inherited context. Read only what this prompt names.

## Important — empty patches are forbidden

The previous attempt returned an empty patch envelope and claimed the fix was already in place. That was wrong. The current on-disk state of `super-gsd/templates/plan-schema-v2.json` (which is in your read-pack) DOES contain the two lines that must change. Verify before declaring no-op.

## Exact current state — these substrings ARE in the read-pack

**Substring A** (lines 8–13 of plan-schema-v2.json):
```
"errorMessage": {
    "required": {
      "semantic_acceptance_criteria": "plan must declare 'semantic_acceptance_criteria' array with >=1 entry (SCHEMA-09)"
    },
    "minItems": "plan 'semantic_acceptance_criteria' must contain >=1 entry (SCHEMA-09)"
  },
```

**Substring B** (lines 20–25 of plan-schema-v2.json):
```
"semantic_acceptance_criteria": {
      "type": "array",
      "minItems": 1,
      "items": { "$ref": "#/definitions/semantic_ac" },
      "description": "Each entry: a falsifiable claim that a real-world input produces a specific outcome (DLB-07, SCHEMA-09)."
    },
```

## Required target state — your patch must produce these substrings

**Substring A becomes** (removes the `"minItems": ...` line and its trailing comma from the previous line):
```
"errorMessage": {
    "required": {
      "semantic_acceptance_criteria": "plan must declare 'semantic_acceptance_criteria' array with >=1 entry (SCHEMA-09)"
    }
  },
```

**Substring B becomes** (gains an `errorMessage` block between `items` and `description`):
```
"semantic_acceptance_criteria": {
      "type": "array",
      "minItems": 1,
      "items": { "$ref": "#/definitions/semantic_ac" },
      "errorMessage": {
        "minItems": "plan 'semantic_acceptance_criteria' must contain >=1 entry (SCHEMA-09)"
      },
      "description": "Each entry: a falsifiable claim that a real-world input produces a specific outcome (DLB-07, SCHEMA-09)."
    },
```

## Verification you must perform before declaring done

Before returning your patch envelope:

1. Grep your read-pack for the literal string `"minItems": "plan 'semantic_acceptance_criteria' must contain >=1 entry (SCHEMA-09)"`. It MUST appear exactly twice in your output: once being removed in `Substring A`, once being added inside `Substring B`'s new `errorMessage` block.
2. Confirm the patch diff has at least 5 `-` lines and at least 5 `+` lines.
3. If your patch is empty, you misread the read-pack. Re-read it and produce the diff.

## Report format

```
PATCH_BEGIN
<unified diff with the two substitutions above>
PATCH_END
REPORT_BEGIN
FILES_CHANGED:
  super-gsd/templates/plan-schema-v2.json (modified)
VERIFICATION:
  Patch contains the removal at Substring A and the addition inside Substring B; "minItems" appears at the expected positions.
DEVIATIONS: <none or list>
BLOCKERS: <none or describe>
ONE_LINER: Move minItems errorMessage from plan level to semantic_acceptance_criteria property level so ajv-errors emits SCHEMA-09 for empty arrays.
REPORT_END
```

Empty PATCH_BEGIN/PATCH_END blocks will be rejected by the orchestrator.
