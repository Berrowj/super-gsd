SUCCESS: The process with PID 40008 (child process of PID 22264) has been terminated.
SUCCESS: The process with PID 11944 (child process of PID 22264) has been terminated.
SUCCESS: The process with PID 36776 (child process of PID 22264) has been terminated.
PATCH_BEGIN
diff --git a/super-gsd/templates/plan-schema-v2.json b/super-gsd/templates/plan-schema-v2.json
index 0000000..0000000 100644
--- a/super-gsd/templates/plan-schema-v2.json
+++ b/super-gsd/templates/plan-schema-v2.json
@@ -4,7 +4,6 @@
   "description": "Canonical YAML-frontmatter schema for SGSD v2 PLAN.md files",
   "type": "object",
   "required": ["schema_version", "tasks", "semantic_acceptance_criteria"],
-  "required": ["schema_version", "tasks", "semantic_acceptance_criteria"],
   "additionalProperties": true,
   "errorMessage": {
     "required": {
@@ -13,20 +12,8 @@
     "minItems": "plan 'semantic_acceptance_criteria' must contain >=1 entry (SCHEMA-09)"
   },
-  "errorMessage": {
-    "required": {
-      "semantic_acceptance_criteria": "plan must declare 'semantic_acceptance_criteria' array with >=1 entry (SCHEMA-09)"
-    },
-    "minItems": "plan 'semantic_acceptance_criteria' must contain >=1 entry (SCHEMA-09)"
-  },
   "properties": {
-    "semantic_acceptance_criteria": {
-      "type": "array",
-      "minItems": 1,
-      "items": { "$ref": "#/definitions/semantic_ac" },
-      "description": "Each entry: a falsifiable claim that a real-world input produces a specific outcome (DLB-07, SCHEMA-09)."
-    },
     "schema_version": {
       "type": "integer",
       "enum": [2],
@@ -78,23 +65,6 @@
       }
     },
-    "semantic_ac": {
-      "type": "object",
-      "required": ["input", "expected_outcome", "verification_cmd"],
-      "additionalProperties": true,
-      "errorMessage": {
-        "required": {
-          "input": "semantic_acceptance_criterion must declare 'input' (SCHEMA-10)",
-          "expected_outcome": "semantic_acceptance_criterion must declare 'expected_outcome' (SCHEMA-10)",
-          "verification_cmd": "semantic_acceptance_criterion must declare 'verification_cmd' (SCHEMA-10)"
-        }
-      },
-      "properties": {
-        "input": { "type": "string", "description": "Description of the real-world input the verification command exercises." },
-        "expected_outcome": { "type": "string", "description": "What the system must produce for the input to pass." },
-        "verification_cmd": { "type": "string", "description": "Shell command that runs against real data and exits 0 iff expected_outcome holds." }
-      }
-    },
     "task": {
       "type": "object",
-  "required": [
+      "required": [
         "id",
         "agent",
         "model",
diff --git a/super-gsd/tools/plan-schema/validate.test.cjs b/super-gsd/tools/plan-schema/validate.test.cjs
index 0000000..0000000 100644
--- a/super-gsd/tools/plan-schema/validate.test.cjs
+++ b/super-gsd/tools/plan-schema/validate.test.cjs
@@ -83,7 +83,7 @@ for (const testCase of cases) {
   // Arrange
   const command = {
     file: process.execPath,
-    args: [validator, testCase.target]
+    args: [validator, '--plan-file', testCase.target, '--project-dir', repoRoot]
   };
 
   // Act
PATCH_END
REPORT_BEGIN
FILES_CHANGED:
  super-gsd/templates/plan-schema-v2.json (modified)
  super-gsd/tools/plan-schema/validate.test.cjs (modified)
VERIFICATION:
  Not run per executor constraint: do not call tools.
DEVIATIONS:
  Fixtures already existed in read-pack, so patch only fixes schema duplication and test invocation.
BLOCKERS:
  None.
ONE_LINER:
  Deduplicates SCHEMA-09/SCHEMA-10 schema entries and makes the fixture test runner call validate.cjs with required CLI args.
REPORT_END

SGSD_PATCH_APPLY: success
SGSD_PATCH_MODE: read-pack
