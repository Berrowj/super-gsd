FILES_CHANGED / `super-gsd/tests/propagation-readiness/assert-dual-root-resolvers.cjs` — isolated missing-corpus fixture path.

VERIFICATION / Exact command: exit 1, 13 pass/30 fail; all bash-backed spawns returned `exit=null`. `node --check` passed.

DEVIATIONS / None; no commit.

BLOCKERS / Sandbox blocked child bash spawns, including missing-corpus verification.

ONE_LINER / Fixture contamination fixed with one path-only change.
