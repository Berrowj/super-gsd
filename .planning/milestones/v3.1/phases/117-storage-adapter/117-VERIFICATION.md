---
phase: 117
phase_name: Chronicle Storage Adapter (VTP-first / local-fallback)
milestone: v3.1
status: PASS
verdict: PASS
completed_at: 2026-05-21
sacs_total: 11
sacs_passed: 11
struct_asserts: 3
struct_passed: 3
warn_softgate: 2
files_created: 4
files_modified: 1
total_assertions: 70
total_passed: 70
deviations: 2
deviation_class: INFO
---

# Phase 117 — Chronicle Storage Adapter — VERIFICATION

## Summary

P117 ships the chronicle publisher + VTP-MCP adapter stub + local atomic file-tree writer + index ledger format + self-test extension. All 11 SAC + 3 STRUCT-P117 PASS. Cumulative 70/70 assertions green across the full v3.1 chronicle pipeline (P113 schema → P114 builder → P115 renderer → P116 validator → P117 publisher).

```
P114 ledger:    23/23 PASS
P115 additions: 17/17 PASS
P116 additions: 16/16 PASS
P117 additions: 14/14 PASS (11 SAC + 3 STRUCT)
WARN STRUCT-P115-24-P113-DOGFOOD + DOGFOOD-P113 (expected; no v3.1 CMBs in ledger yet)
TOTAL: 70/70 PASS
```

## Files

### Created (4)
- `super-gsd/tools/chronicle/publish.cjs` — orchestrator. Loads bundle → validates context + manifest against P113 schemas via ajv+ajv-errors → refuses ungrounded (exit 2 unless --force) → computes SHA-256 content_hash → checks idempotency via index ledger → routes auto/vtp/local → invokes adapter → appends index row → prints `PUBLISHED <target> <location>`.
- `super-gsd/tools/chronicle/storage-vtp.cjs` — VTP-MCP adapter STUB. `probe()` returns `{available: false, reason: 'vtp_mcp_routing_not_yet_wired'}` unless `process.env.SGSD_VTP_MCP_URL` is set (then attempts HTTP reachability). `upsert()` throws or POSTs based on env config. Real wiring deferred (out of v3.1 scope; documented).
- `super-gsd/tools/chronicle/storage-local.cjs` — local writer. Resolves `<localRoot>/{milestone_id}/P{phase_id}/`; mkdirSync recursive; writes 4 files via atomic tmp+rename: `chronicle-context.json`, `chronicle.html`, `manifest.json`, `content-hash.txt`. Mirrors execution-receipt.cjs pattern.
- `super-gsd/tools/chronicle/fixtures/sample-publish-bundle.json` — synthetic bundle. Combines P114's golden sample-chronicle-context.json + schema-valid manifest + minimal HTML + `REPORT_GROUNDED`. Regenerated by orchestrator post-Codex (Codex's initial bundle used wrong context shape).

### Modified (1)
- `super-gsd/tools/chronicle/run-self-test.cjs` — extended with SAC-P117-01..11 + STRUCT-P117-21..23 (preserved all 56 prior assertions).

## DLB-11 R5 invariant coverage

| Invariant | Mechanism | Status |
|---|---|---|
| VTP-first / local-fallback | `--storage-target auto` (default) probes VTP; falls back to local on failure | ✓ SAC-04 |
| Never blocks on VTP availability | Stub probe defaults to unavailable; auto routes to local; explicit `--storage-target vtp` fails fast (exit 4) without polluting local | ✓ SAC-05 |
| CMB references by-ID, never bodies | Bundle.chronicle_context emitted by P114 builder uses by-reference citations; publish writes byte-identical context.json | ✓ SAC-06 |
| Index ledger append-only | One JSON line per publication; idempotent on content_hash collision (no duplicate rows) | ✓ SAC-02, SAC-08 |
| Atomic local writes | tmp+rename pattern (writeFileSync + renameSync) | ✓ STRUCT-21 |
| Content_hash de-duplication | SHA-256 over canonical JSON; same bundle → same hash; idempotent re-publish | ✓ SAC-02, STRUCT-22 |
| Refuses ungrounded bundles | `validator_verdict !== 'REPORT_GROUNDED'` → exit 2 (no files written) | ✓ SAC-03 |
| Refuses schema-invalid bundles | Validates context + manifest before writing → exit 1 | ✓ SAC-07 |

## Deviations

**INFO-1 — VTP-MCP upsert is a STUB (per CONTEXT design).** Real VTP wiring is deferred out of v3.1 scope. The probe returns `{available: false}` by default; routing logic is fully implemented + tested via env-overridable code path. When real VTP wiring lands later, only `storage-vtp.cjs` upsert body needs replacing — routing/idempotency/index-ledger paths are stable.

**INFO-2 — Orchestrator-regenerated bundle fixture (same recurring pattern from P116).** Codex's initial `sample-publish-bundle.json` had chronicle_context using stale shape (`phase: {id, title}` object instead of flat `id`/`title` strings; missing required root fields `chronicle_version` / `source` / `diagrams` / `assets`; manifest missing `manifest_version` + chronicles[] items missing `hash`). Orchestrator regenerated using P114's golden sample-chronicle-context.json + hand-authored schema-valid manifest. Mechanical fixture authoring; no semantic deviation from P117 contract.

## Soft warnings

**WARN — DOGFOOD-P113 + STRUCT-P115-24-P113-DOGFOOD still skipped.** Both fire when v3.1 phases emit CMBs into the mesh ledger. v3.1 phases are not yet wired to CMB emission (would require modifying P113-P115 source files to add execution_receipt writers; out of scope here). Dogfood becomes load-bearing at v3.1 milestone close (P119) when the milestone chronicle attempts to roll up all 7 phases. P118 (cockpit) may surface this as an inline warning.

## ATC LITE self-review

- First Principles: storage adapter completes the chronicle pipeline (validator → publisher) ✓
- Delete: 4 files matches plan; no bonus ✓
- Simplify: orchestrator dispatches to flat adapter modules; each adapter ~150 LOC ✓
- Accelerate: synchronous file ops; <50ms publish time on local ✓
- Automate: idempotent on content_hash; no manual de-dup ✓
- Validate: 14-assertion P117 surface area; 70-assertion cumulative ✓
- Anti-slop: every code path exercised by ≥1 SAC; no orphan branches

## MUDA self-review

- Overproduction: 4 created + 1 modified = 5 ops matches plan
- Inventory: every SAC tied to one code path; every code path exercised
- Defects: 1 orchestrator-applied fix (bundle fixture regeneration)
- Motion: no cross-file refactoring
- Waiting: 3-task split correct; t1 adapters parallel-safe; t2 publish depends on both; t3 self-test depends on all
- Over-processing: VTP stub keeps adapter contract stable for future real wiring (deliberate; not over-engineering)
- Transport: publish.cjs + adapters co-located; index ledger at canonical `.planning/chronicles/INDEX.jsonl`

## Next phase

P118 — Cockpit Integration. Adds chronicle links + Fog Score badge to cockpit-state output. May inherit v2.9 DEFERRED-2 (cockpit 12th section under frozen-array Lock-13) — decision: extend the cockpit OR add a sidecar. Fog Score derivation lands here (dispatches + token spend + commit count + review loops + disputed claims).

## Provenance

- Codex executor: read-pack patch mode; 4 files + 1 modify landed first pass
- Orchestrator-applied fix: bundle fixture regeneration to conform to P113 schema shape (recurring Codex pattern — its draft fixtures use a different chronicle_context shape than the canonical builder output)
- Self-test execution: 70/70 PASS post-fix
