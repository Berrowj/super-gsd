Design approval required before editing:

- Recommended: make `mkContext(explicitProjectDir)` preserve `path.resolve(explicitProjectDir)` exactly; retain planning-root discovery only when no destination is supplied. Check and repair remain shared.
- Add deterministic ancestor-`.planning` coverage with the existing spaced fixture path.
- Restore the retired copy-order assertion against the actual Codex copy call.
- Keep refusal text and pre-writer ordering unchanged.

Alternatives are installer-specific path flags or special-casing vendored projects; both add coupling and risk detector/repair drift.

Approve the recommended design?
