FILES_CHANGED / `super-gsd/scripts/lib/hook-registration-preflight.cjs`

VERIFICATION / Syntax PASS; `preflight-static` PASS; reconstructed spaced-path fixture throws exactly 3× `hook_registration_missing`; diff check PASS.

DEVIATIONS / Guard fixture unchanged. Full spawn-bound case is sandbox-blocked (`spawnSync EPERM`).

BLOCKERS / None for required static proof.

ONE_LINER / Shared path parsing now accepts quoted managed-row paths before existence validation; no commit created.
