# P150 REVIEW ROUND 4 — confirm round-3 findings closed

Codex reviewer, round 4 (final for the automatable half). Round 3 (below) had 4 CRIT + 1 WARN open. Fixes landed: exact-URL publication + push-URL allowlist (evil-host negative test added); verify-before-mutate staged restore (same-path tamper + live-untouched assertions); drift-status-10 continue handling with status-10 fixture; .gitattributes eol enforcement; PII scrub to 0 tracked occurrences (operator-ruled scope: current files + test-forward, history accepted) with privacy test over all phase artifacts. Host battery fully green (24/10/5/12/10/10).

Verify each closed with file:line evidence. MANDATORY: one FINDINGS_DETAIL line per finding (CLOSED or open); a report without detail lines is INVALID. If all closed and no NEW critical surface, SPEC_VERDICT: pass.

Report contract — ALL exact lines: FINDINGS/CRITICAL/WARNINGS/PASS_RATE/ONE_LINER/SPEC_VERDICT: pass|fix_required, then FINDINGS_DETAIL lines.

## Round 3 verdict
FINDINGS: 5
CRITICAL: 4
WARNINGS: 1
PASS_RATE: 44%
ONE_LINER: Publication origin, rollback integrity, outgoing PII, and devcp drift handling remain unsafe despite the green host battery.
FINDINGS_DETAIL: [CLOSED] [ROUND2-1 security/origin] `super-gsd/scripts/sgsd-update.sh:43-67,117` and `super-gsd/scripts/sgsd-update.ps1:25-38,126` now enforce exact canonical fetch-URL allowlists before `ls-remote` or `fetch`; `super-gsd/tests/propagation/sgsd-update-contract.test.cjs:283-315,521-532` covers both shells and both modes.
FINDINGS_DETAIL: [CRITICAL] [ROUND2-2 security/publication-origin] The executable publication ceremony is present at `PROPAGATION.md:92-332`, but its allowlist at `PROPAGATION.md:99-106,156-161` accepts any host whose path ends in `Berrowj/super-gsd`, and the push at `PROPAGATION.md:296` never validates a distinct push URL. For example, `https://evil.example/Berrowj/super-gsd.git` matches. `runbook-contract.test.cjs:149-180` locks in the weak regex, so canonical `origin/master` can remain unpublished.
FINDINGS_DETAIL: [CLOSED] [ROUND2-3 recovery/boundary] `sgsd-global-snapshot.sh:103-264` now pins and analyzes the complete installer mutation contract, while `sgsd-global-snapshot.sh:532-540` rejects snapshot creation unless `~/.claude/get-shit-done` already exists; corresponding negative fixtures are at `global-snapshot-contract.test.cjs:240-291`.
FINDINGS_DETAIL: [CRITICAL] [ROUND2-4 security/rollback] Symlinked storage paths are resolved at `sgsd-global-snapshot.sh:266-279,493-504`, but archive validation at `sgsd-global-snapshot.sh:360-430` compares only a mutable adjacent checksum and member names—not manifest type, link target, mode, or content hashes. Live targets are quarantined and the archive extracted at `sgsd-global-snapshot.sh:505-511`; content is compared only afterward at `sgsd-global-snapshot.sh:512-514`. A same-path tampered file or symlink with a refreshed sidecar therefore reaches live state before rejection, breaking rollback safety.
FINDINGS_DETAIL: [CLOSED] [ROUND2-5 trust-proof] `PROPAGATION.md:389-470` now performs a real Codex dispatch, captures the byte offset and UTC start, checks process status and forbidden-file absence, reads only appended bytes, parses JSONL, and requires a fresh matching block event. `runbook-contract.test.cjs:185-264` validates the sequence and executes it against append-only history.
FINDINGS_DETAIL: [CRITICAL] [ROUND2-6 privacy/no-PII] `PROPAGATION.md` itself is scrubbed and derives the root at `PROPAGATION.md:29-32`, but the pushed P150 history still contains the identifiable account in 21 tracked files, including 22 matching lines in `150-01-PLAN-LOCKED.md:85,134,305,601-643,943,1196`. The privacy test checks only `PROPAGATION.md` at `runbook-contract.test.cjs:352-356`, so the locked no-PII publication criterion remains violated.
FINDINGS_DETAIL: [CRITICAL] [ROUND2-7 linux-install/restart-coherence] Project CWD, canonical cockpit resolution, and AC-150d fields are repaired at `PROPAGATION.md:521-534`, `start-cockpit-server.sh:116-125,266-269`, and `sgsd-devcp-restart-evidence.sh:345-360`. However, the remote updater enables `set -e` and runs `--check` before update at `PROPAGATION.md:519-534`; expected upstream drift returns status 10 at `sgsd-update.sh:137-144`, aborting before installation. The fixture at `runbook-contract.test.cjs:286-304` always returns zero and misses this path.
FINDINGS_DETAIL: [WARNING] [ROUND2-8 anti-slop/salvage] The tracked `.orig` files are removed, but branch-wide line-ending churn remains across `super-gsd/install.sh:1-889`, `super-gsd/scripts/lib/sgsd-readiness.ps1:1-279`, `super-gsd/scripts/sgsd-onboard.ps1:1-375`, and `super-gsd/tools/feature-propagation/audit.cjs:1-894`; the raw diff is 2,291 additions/2,168 deletions versus only 132 additions/9 deletions when end-of-line whitespace is ignored.
FINDINGS_DETAIL: [CLOSED] [ROUND2-9 test-quality] The original trust/schema false-greens are repaired: `runbook-contract.test.cjs:214-264` executes the documented trust ceremony, while `restart-evidence-contract.test.cjs:153-215` executes the emitted AC-150d JSON builder and `restart-evidence-contract.test.cjs:217-260` executes canonical-cockpit selection. The separate drift-status blind spot is accounted for in ROUND2-7.
