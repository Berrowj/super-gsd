FINDINGS: 4
CRITICAL: 2
WARNINGS: 2
PASS_RATE: 19/19
ONE_LINER: fix_required; real-git fixture path is realistic, but cleanup safety and source/config predicate coverage still violate ATC/spec edges.
FINDINGS_DETAIL: [CRIT] [writer_destination] `assert-real-commit-gate.cjs:159` `cleanupFixture(tempRoot)` can delete any caller-supplied tree because it derives containment from `dirname(tempRoot)` + `basename(tempRoot)` instead of proving the target is an internally-created `os.tmpdir()` fixture.
FINDINGS_DETAIL: [CRIT] [predicate_correctness] `sgsd-artifact-conventions.cjs:69` uses `path.extname()` for config detection, so root dot-config paths such as `.env` resolve to no extension and are treated as `not_source`, letting config/runtime changes avoid warnings.
FINDINGS_DETAIL: [WARN] [silent_success] `sgsd-artifact-conventions.cjs:528` drops invalid/traversal staged entries before record creation; that breaks the per-path record contract and can make bad staged input disappear rather than return reason-coded evidence.
FINDINGS_DETAIL: [WARN] [predicate_correctness] `sgsd-artifact-conventions.cjs:454` has inconsistent case behavior: exact and `/**` allowed-file checks are case-sensitive, while wildcard checks are case-insensitive, creating Windows/cross-platform false misses or over-matches.
