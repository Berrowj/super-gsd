FINDINGS: 2
CRITICAL: 1
WARNINGS: 1
PASS_RATE: 4/6
ONE_LINER: fix_required; writer path is not caller-controlled and mandatory emit precedes suggestions, but lexicon gaps still create silent governance misses.
FINDINGS_DETAIL: [CRITICAL] [lexicon/silent-success] Planning-shaped prompts such as "how would you approach the migration?", "let's scope the next milestone", and "can you make a roadmap for P149?" do not match any planning route, so the hook exits quietly with no `/sgsd-triage`; execution-shaped prompts like "evaluate the failing test and patch it" can also false-route because `^\\s*evaluate\\b` is unconditional.
FINDINGS_DETAIL: [WARNING] [anti-slop] The registry carries fields/routes the classifier does not use or enforce: `schema_version`, `registry_version`, `owner_phase`, `intent`, `predicate.match`, and `kind: none` routes only create parser surface area; this hand-rolled YAML parser is fragile compared with a JSON sidecar or a stricter consumed schema.
