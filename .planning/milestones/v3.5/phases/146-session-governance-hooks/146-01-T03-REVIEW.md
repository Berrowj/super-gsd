FINDINGS: 3
CRITICAL: 2
WARNINGS: 1
PASS_RATE: 5/8
ONE_LINER: fix_required + ATC fails never-throw/blocking edge cases; path derivation, shared-helper consumption, settings/env silence, and handoff pairing otherwise pass.
FINDINGS_DETAIL: [CRITICAL] [ATC-3 never-throw] `main()` has no top-level guard: `resolveContext(payload)` can propagate `findSgsdRoot`, `readState(ctx.root)` is outside any try, and catch blocks call `logStatePhaseMissing()` unguarded, so `logGateEvidence()` can still throw with a stack and nonzero exit.
FINDINGS_DETAIL: [CRITICAL] [spec first-turn-context] `emitGovernanceContext()` runs optional checkpoint/memory briefing before `console.log()`. If checkpoint read/parsing hits an unexpected filesystem error, the hook suppresses emission and the SessionStart gets no governance contract.
FINDINGS_DETAIL: [WARNING] [ATC-6 anti-slop] Failure catch paths reuse `state_phase_missing` rows for `session_start_governance_failed` and `session_start_handoff_pairing_failed`; with missing phase plus a later handoff failure this can append misleading duplicate missing-phase evidence.
