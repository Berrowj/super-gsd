FINDINGS: 3
CRITICAL: 1
WARNINGS: 2
PASS_RATE: 5/8
ONE_LINER: fix_required + ATC found repo-local CLI path validation gap; overlay events/absolute args mostly comply
FINDINGS_DETAIL: [CRITICAL] [security/spec] `merge-settings.js --repo-local-hooks` trusts caller-supplied `overlayPath`, `targetPath`, and `repoRoot` (`super-gsd/scripts/merge-settings.js:426`). `mergeSettingsFiles` then reads and writes that target (`:292`, `:401`) without enforcing `<repo>/.claude/settings.json`, so a bad invocation can write arbitrary JSON paths, including `~/.claude/settings.json`, and can copy top-level `env` if a home settings file is passed as overlay (`:356`).
FINDINGS_DETAIL: [WARNING] [idempotency] Dedupe is not actually command+matcher: `hookLaunchKey` includes `args` (`super-gsd/scripts/merge-settings.js:59`), so an existing same-command/same-matcher hook with stale absolute args from a prior repo location is silently kept and a new entry is appended (`:389`). Exact matcher string equality also misses semantically identical reordered matchers (`:71`).
FINDINGS_DETAIL: [WARNING] [security/self-test] The repo-local self-test overwrites `process.env.HOME` and `process.env.USERPROFILE` (`super-gsd/scripts/merge-settings.js:242`) but never restores them in `finally` (`:283`). This is process-local and exits immediately, so parent-shell leakage is unlikely, but an assertion path still leaves redirected env for any later same-process cleanup/reporting added in the future.
