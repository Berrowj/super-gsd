FINDINGS: 2
CRITICAL: 1
WARNINGS: 1
PASS_RATE: 7/9
ONE_LINER: fix_required: spec fixes mostly land, but ATC finds a handoff depth under-count path and a watchdog caller-derived write surface.
FINDINGS_DETAIL: [CRITICAL] [handoff-latch] `sgsd-stop-handoff.sh` only counts prior `reason === "spawned"` rows, but the `unexpected_auto_stop` branch also spawns a new Claude session while logging `reason:"unexpected_auto_stop"`, so repeated unexpected-stop recoveries keep proposing depth 1 and can bypass `max_chain_depth`.
FINDINGS_DETAIL: [WARNING] [writer-destination] `autopilot-watchdog/check.cjs --write` derives output paths directly from caller-supplied `--project-dir` and will create `.planning/metrics/autopilot-watchdog.json` even when `check()` returned `reason: "no .planning directory"`, leaving a caller-selected write destination in scope.
