# Phase 144: VTP Evidence Framing

**Generated by:** sgsd-triage Step 0
**Generated at:** 2026-08-02T14:05:00Z
**Composer version:** vtp-context-composer.cjs (TIERS, triage slice)

## Framing

- **raw_query:** Should an agent harness enforce quality gates by hard-blocking every source edit, or is making silent-skip failures loud enough via reporting? Gate interference, stacked gate cost, and regression risk versus units shipped with no audit evidence.
- **selected_query:** `markdown patterns for .planning/analyses/2026-08-02-always-on-gates-and-context-handover-PLAN.md`
- **retrieval_mode:** `architecture_hybrid`
- **intent_type:** `research_lookup`
- **answer_shape:** `evidence_bundle`
- **reflection_verdict:** `null` (recorded verbatim per skill Step 6)
- **routing_decisions:** `expand_query_multi_hypothesis`, `prioritize_code_symbol_lookup`, `deprioritize_generic_advice`

## Routing quality note — GATE UNDERPERFORMED

`vtp_route_and_retrieve` rewrote the question into *"markdown patterns for
&lt;active_file&gt;"*, latching onto `context.active_file` rather than `raw_query`.
It classified a design-tradeoff question as `research_lookup` and returned
meeting transcripts (`doc:b0ba4deca58c`, `doc:348050f303b6`,
`doc:1380f30577a5`) plus general AI textbooks — none of the harness papers the
question is actually about. `reflection` was `null`, so nothing caught it.

Per skill Step 10 (graceful-fail): not retried, not blocked. Fell through to
`vtp_search_research`, which returned the correct corpus immediately.

**This is itself a silent-skip finding**, same family as the day's other six:
the enrichment gate produced worse evidence than the direct search and reported
no failure. Candidate input to the board's Decision 3.

## Top Evidence Doc-IDs (references only — not content)

Via `vtp_search_research` (the tool that actually resolved the question):

1. `agentic-harness-engineering-observability-driven-automatic-evolution-of-coding-a` (AGEN-SUBSTRATE, arXiv 2604.25850v1) — score 0.347 / 0.307. Source of AHE-P-09 (stacked gates interfere) and AHE-P-10 (regression prediction 11.8%/11.1%). **Directly load-bearing for Decision 3 — it is the primary source of the argument AGAINST the proposed fix.**
2. `shift-up-se-guardrails-for-ai-native-development` (SHIF-SUBSTRATE, arXiv 2604.20436) — score 0.293. Guardrails in AI-native development; source of "prose-only artifacts the agent never references lose all benefit".
3. `forage-v2-knowledge-evolution-autonomous-agents` (FORA-SUBSTRATE, arXiv 2604.19837) — score 0.348. Self-evaluation reported 100% completion at 15.9% actual recall; structural separation over better prompting.

Secondary (lower relevance, noted not used): `metis-mentoring-engine-for-thoughtful-inquiry-solutions`, `architecture-matters-more-than-scale`, `fsfm-selective-forgetting-agent-memory`, `kairos-stateful-context-aware-agentic-inference`.

## Re-query Contract

Downstream agents must re-query VTP for tier-specific evidence at call time
using these slugs as seeds — `vtp_get_research &lt;slug&gt;` for full enrichment, or
`vtp_research_gate` for debate-grounded recommendations. This file holds framing
only; evidence is always fresh-at-use.

**Routing caution for downstream agents:** prefer `vtp_search_research` /
`vtp_search_substrate` for paper content. `vtp_route_and_retrieve` mis-routed
this query via `active_file`. Do not pass a filename in `context.active_file`
when the question is conceptual.
