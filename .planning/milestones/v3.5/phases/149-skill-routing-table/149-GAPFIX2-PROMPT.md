# P149-gapfix2 — Close verifier round-2 findings (execution semantics)

Fresh SDD implementer (Codex gpt-5.6-sol/xhigh). Fix EXACTLY these three round-2 verifier findings. Surgical constraint applies.

## Findings (file:line cited)
FINDINGS_DETAIL: [CRITICAL] [logic] super-gsd/scripts/lib/orchestrator-hooks.cjs:295 treats every non-zero dispatch status as `execution_failed`, and super-gsd/skills/sgsd-orchestrate/SKILL.md:1859 requires repairing that outcome before Step 6.7. This misclassifies legitimate skill verdicts: super-gsd/tools/release-readiness/score.cjs:1118 intentionally exits 1 below 70 (fresh v3.5 probe: 60/100, exit 1), while super-gsd/scripts/sgsd-muda-audit.sh:25 intentionally uses exits 1/2 for non-blocking WARN/FAIL findings. Fired routes execute, but valid negative results can deadlock phase close.
FINDINGS_DETAIL: [WARNING] [architecture] super-gsd/scripts/lib/skill-routing-registry.cjs:659 calls its fallback assertion “deep” but projects only skill, moment, modes, cooldown, and gate_ref, omitting the newly execution-critical dispatch object. Current 24-row semantic parity and the sgsd-audit cooldown are equal, but an adversarial in-memory dispatch change to `echo` left this self-test projection equal, so dispatch drift remains mechanically green.
FINDINGS_DETAIL: [WARNING] [logic] super-gsd/scripts/lib/skill-routing-registry.cjs:330 accepts any non-empty command, while super-gsd/scripts/lib/orchestrator-hooks.cjs:967 replaces every dispatch with a status-0 stub and line 991 asserts only that the command is non-empty. A no-op command therefore passes A10 without executing a skill; additionally, super-gsd/skills/sgsd-orchestrate/SKILL.md:1533 already runs MUDA before the line-1843 consult executes the same YAML dispatch again, creating a concrete duplicate execution surface.

## Fix requirements
1. CRIT (exit-code semantics): add per-route exit policy to skill-routing.yaml dispatch objects (e.g. success_exits or verdict_exits list). Outcome classification: exit in success list -> executed; exit in verdict list (e.g. muda 1/2, readiness 1) -> executed_with_findings (evidence row carries the exit + meaning, phase close NOT blocked, matches DLB-02 detect-and-record); anything else -> execution_failed. Update SKILL.md:~1859 so only execution_failed requires attention before Step 6.7 — executed_with_findings is logged and continues.
2. WARN (parity projection): include the full dispatch object (command + exit policy) in the fallback deep-parity self-test projection so dispatch drift fails self-test.
3. WARN (no-op dispatch + duplicate MUDA): (a) validate at load/self-test that each dispatch command's target script/CLI exists in the repo (reject echo/no-op stubs); extend A10 to execute one real dispatch against a fixture or verify target-file existence rather than status-0 stubs. (b) Resolve the duplicate MUDA execution surface: the consult is the single execution point — its muda route already carries gate_ref MUDA-waste-audit; ensure SKILL.md Step 6.55 text defers execution to the consult (reference, not a second invocation), or the consult skips when the gate fired earlier in the same close. Pick the minimal coherent option and state it in DEVIATIONS if prose interpretation was required.

## Verify before reporting: registry --self-test, hooks --self-test, one consult --execute round on a scratch phase demonstrating executed_with_findings classification (e.g. muda exit 1). Report results.

## Report contract: FILES_CHANGED / VERIFICATION / DEVIATIONS / BLOCKERS / SCRIPTS_CREATED / ONE_LINER / STATUS
