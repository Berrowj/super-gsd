# v1.2 Evidence-First Sharpening — Archive

**Milestone:** v1.2 Evidence-First Sharpening
**Phases:** 9-13
**Status:** Shipped (retroactive close 2026-04-24 via v1.4 CLEAN-06)
**Git tag:** `v1.2` at commit `0191168`

This archive is the authoritative record of Phases 9-13 delivered under milestone v1.2.
Content drawn from `.planning/ROADMAP.md` Phase Details (Phases 9-13) + `.planning/MILESTONES.md` v1.2 narrative.

---

## Milestone Summary

v1.2 converted empirical bypass evidence (Phase 9) into enforceable policy (Phase 10), codified the plan schema contract (Phase 11), sharpened the orchestrator machinery (Phase 12), and closed with governance upgrades that activated the v1.3 handoff path (Phase 13).

**Retroactive close note:** v1.2 shipped per ROADMAP.md checkboxes but was never formally closed via `/gsd-complete-milestone v1.2`. The v1.3 Multimodal Review milestone proceeded on top. This archive + git tag `v1.2` constitute the retroactive close ceremony recorded during v1.4 CLEAN-06.

---

## Phase Summary Checklist

- [x] **Phase 9: ATC-147-Evidence** — SHIPPED 2026-04-22 — 3 plans / 12 commits; PASS-WITH-DEVIATIONS: classifier landed 4-bucket split of 10 findings matching §Q7 mapping exactly, gate-bypass audit delivers 18,940 upper / 9,340 lower token bounds for Phase 10 consumption, v1.2 INTENT.md + SHA-pinned evidence registry + 7-invariant mechanical verifier; ATC WARN: 0 critical, 2 verifier coverage gaps flagged for Phase 10 rubric design
- [x] **Phase 10: Gate Policy** — SHIPPED 2026-04-22 — 3 plans / 12 commits; PASS: 11-row gates.yaml populated per D-01..D-09 + D-12, 3 new lib modules in super-gsd/scripts/lib/ (predicate-eval 10-op evaluator, gates-registry cache-singleton with shouldFire, edge-guard transition-wrapper with --self-test), 9 SKILL.md call sites wired, 09-verify.mjs retrofitted with WR-01/02 invariants, config.byterover deleted; ATC WARN 0 critical / 9.67/10 anti-slop / 3 warnings forwarded to Phase 12 ergonomics; MUDA 1 non-blocking FAIL inventory; cross-repo operator action: 7 keys to core.cjs KNOWN_TOP_LEVEL pending
- [x] **Phase 11: Plan Schema v2** — SHIPPED 2026-04-21 — 6 plans / 18 commits; PASS after 11-06 gap closure: WR-01..05 + IN-01 all closed, 0 ATC warnings remain; IN-03 distribution gap deferred to Phase 12+
- [x] **Phase 12: Machinery** — SHIPPED 2026-04-22 — 6 plans / ~24 commits; PASS: 3 new lib modules (classifier-cache per-plan sidecar, dispatch-planner Kahn topo-sort, context-gauge opt-in mechanical 85% threshold), SKILL.md integrations across 5 sections, checkpoint template +4 fields, adversarial verifier Step 9.6 with verifier_adversarial_rate=0.2, patch-gsd-tools-known-keys.sh idempotent installer shipped + README documented; ATC WARN 0 critical / 8.75/10 anti-slop / 2 warnings deferred to ergonomics sweep; MUDA clean; Agent() parallel fan-out confirmed concurrent in 12-02-00 spike; Phase 10 WR-01/02/03 ergonomics closed; Phase 11 IN-03 still deferred to infra phase
- [x] **Phase 13: Governance** — SHIPPED 2026-04-23 — board registry activated, signed-sum vote synthesis landed, board agents moved to 10-field YAML, decision memo template extended with falsifier/dead-ends/reflection, `sgsd-complete-milestone` added with VTP tiered fallback, orchestrator Step 6.7 auto-trigger wired, Phase 13 verifier green 16/16

---

## Phase 9: ATC-147-Evidence

**Goal:** Close the evidence gap from Phase 147's silent-gate-skip by running a retroactive ATC review and a bypass-cost audit, producing the empirical finding count that Phase 10's keep/kill matrix will key against.

**Depends on:** External `project-clarity-erp` Phase 147 retroactive ATC landing in that repo (out-of-scope-to-this-repo execution).

**Requirements:** ATC-147-01, ATC-147-02, ATC-147-03

**Success Criteria:**
1. A retroactive ATC review exists at `project-clarity-erp/.planning/phases/147-clarity-relay-map-w1/147-ATC-REVIEW.md` with every finding classified as real-bloat / nit / false-positive (ATC-147-01).
2. The v1.2 milestone's evidence registry cross-links to that external review file via a relative or operator-resolvable path (ATC-147-02).
3. A gate-bypass audit enumerates the 9 CLAUDE-OVERLAY gates Phase 147 skipped with a token-cost estimate per gate (ATC-147-03).
4. The finding count is surfaced in a form Phase 10 can consume (keyed against ≥3 / 1-2 / 0 thresholds).

**Plans:** 3 plans

- [x] 09-01-classification.md — Narrow Sonnet classifier + 09-classification.yaml (ATC-147-01)
- [x] 09-02-gate-bypass.md — 9-row gate-bypass token-cost audit YAML (ATC-147-03)
- [x] 09-03-registry-and-intent.md — v1.2 INTENT.md + evidence registry pointer + verify.mjs (ATC-147-02)

---

## Phase 10: Gate Policy

**Goal:** Convert the Phase 9 empirical finding count into an enforceable, per-gate keep/kill/conditional matrix, landed as YAML registry (not prose), and wire an edge-guard layer that catches the Phase-147-style silent skip-drift going forward.

**Depends on:** Phase 9 (needs finding count from ATC-147-01 to fill the matrix's empirical-trigger column).

**Requirements:** GATE-01, GATE-02, GATE-03, GATE-04

**Success Criteria:**
1. Every CLAUDE-OVERLAY gate has a row in the decision matrix declaring it HARD-HALT / SOFT-WARN / CONDITIONAL with the empirical trigger explicit per row (GATE-01).
2. `super-gsd/registry/gates.yaml` exists and encodes per-gate enforcement mode for both per-dispatch Step 9.5 and phase-level Step 6.5 (GATE-02).
3. Every non-ATC gate in the loop has an explicit keep/kill/conditional verdict recorded (GATE-03).
4. An edge-guard enforcement layer emits `.planning/metrics/edge-guard-log.jsonl` rows on every step transition (GATE-04).

**Plans:** 3 plans / 12 commits — PASS

---

## Phase 11: Plan Schema v2

**Goal:** Publish the canonical v2 plan schema as YAML frontmatter, enforce required task fields at plan-load, support documented optional fields with defaults, and establish a backward-compatible fallback so the 146 existing v1 plans keep running unchanged.

**Depends on:** Nothing (can start in parallel with Phase 9 evidence collection).

**Requirements:** SCHEMA-01, SCHEMA-02, SCHEMA-03, SCHEMA-04, SCHEMA-05

**Success Criteria:**
1. `super-gsd/templates/plan-schema-v2.json` exists and defines a `schema_version: 2` YAML-frontmatter contract with a `tasks: [...]` list (SCHEMA-01).
2. The parser enforces required per-task fields — `id`, `agent`, `model`, `files_touched`, `input_contract`, `output_contract`, `hypothesis`, `falsifier`, `stop_rule` (SCHEMA-02).
3. Optional per-task fields (`depends_on`, `known_deadends`, `lessons_path`, `prior_errors_lookup`, `expected_ATC_tier`, `verification_cmd`, `skip_gates: []`) are supported with documented defaults (SCHEMA-03).
4. Plans with no `schema_version` or `schema_version: 1` route through the existing Haiku classifier; v2 plans skip the classifier entirely (SCHEMA-04).
5. `superpowers:writing-plans` emits v2 by default, `sgsd-orchestrate` consumes v2 natively (SCHEMA-05).

**Plans:** 6 plans / 18 commits — PASS

---

## Phase 12: Machinery

**Goal:** Apply four orchestrator-internal sharpenings (Q6a-d) — classifier-skip, parallel/sequential dispatch, checkpoint-schema expansion, and adversarial verifier sampling — to reduce per-phase overhead and catch verifier blind spots.

**Depends on:** Phase 11 (MACH-01/02 consume schema v2 fields), Phase 10 (MACH-01 kill-condition keyed off gate matrix).

**Requirements:** MACH-01, MACH-02, MACH-03, MACH-04

**Success Criteria:**
1. Classifier-skip policy implemented — per-plan cached verdict (Q6a-iii). v1 plans continue to hit the classifier every loop (MACH-01).
2. Parallel/sequential dispatch auto-detection works on v2 plans — orchestrator reads `depends_on` + `files_touched` (MACH-02).
3. Checkpoint schema expanded with `approaches_tried_and_abandoned`, `rules_learned_this_session`, `dispatches_summary`; trigger changed to `(phase_boundary OR plan_boundary) AND context >70%` (MACH-03).
4. Adversarial verifier sampling live — N=20% of verifier "pass" verdicts receive contrarian-challenger second pass (MACH-04).

**Plans:** 6 plans / ~24 commits — PASS

---

## Phase 13: Governance

**Goal:** Apply seven deliberate-skill sharpenings (Q7a-g) — board as registered resource, escalate-not-spawn defaults, confidence-weighted votes, falsifier-bearing memos, structured responses, post-deliberation scoring — and prove the new milestone-close path.

**Depends on:** Phase 10 (gates.yaml registered-resource precedent), Phase 11 (schema-ownership model).

**Requirements:** GOV-01, GOV-02, GOV-03, GOV-04, GOV-05, GOV-06, GOV-07

**Success Criteria:**
1. Minimal-2 default board (Architect + Contrarian) live with escalation policy in `super-gsd/registry/board-members.yaml#escalation_policy` (GOV-01).
2. Board members self-rate 1-5 confidence; CEO synthesis weights votes by confidence rather than raw count (GOV-02).
3. Every decision memo includes `## Falsifier` and `## Dead Ends / Paths Ruled Out`; `super-gsd/templates/decision-memo.md` updated (GOV-03).
4. Board members resolved at deliberation-start from `super-gsd/registry/board-members.yaml` (GOV-04).
5. Milestone-close hook audits every DLB fired since prior close, writes to `.planning/metrics/deliberation-outcomes.jsonl` (GOV-05).
6. Board member responses structured YAML with 10 fields; CEO synthesis rubric-driven (GOV-06).
7. Every CEO memo footer carries post-synthesis reflection pass; cross-fed into GOV-05 scoring log (GOV-07).
8. `sgsd-complete-milestone` live, idempotent, evidence-safe: closes v1.2 through bidirectional VTP and leaves staged v1.3 packet ready.

**Plans:** verifier green 16/16 — PASS

---

## Dependency Chain

```
Phase 9  (ATC-147-Evidence) ──┐
                               ├──► Phase 10 (Gate Policy) ──┐
Phase 11 (Plan Schema v2) ─────┼──► Phase 12 (Machinery)    │
                               │                             ├──► Phase 13 (Governance)
                               └─────────────────────────────┘
```

- **9 → 10:** Phase 10 cannot set gate enforcement modes without Phase 9's empirical finding count.
- **11 → 12:** Phase 12's classifier-skip and parallel/sequential dispatch both consume schema v2 task fields.
- **10 → 12:** Phase 12's classifier kill-condition is keyed off Phase 10's gate matrix.
- **10 → 13:** Phase 13's `board-members.yaml` follows the Phase 10 `gates.yaml` registered-resource precedent.
- **11 → 13:** Phase 13's schema-ownership model for board-members.yaml mirrors Phase 11's plan-schema ownership.

---

*Archive generated 2026-04-24 during v1.4 CLEAN-06. Source: `.planning/ROADMAP.md` Phase Details Phases 9-13 + `.planning/MILESTONES.md` v1.2 narrative.*
